{
  "version": "0.9.16",
  "date": "2026-06-25",
  "headline": "Enumerates the vendored PKI meta-bundle's nested packages in the release SBOM so a vulnerability scan keys on each nested version",
  "summary": "A supply-chain release with no change to client behaviour. The release software bill of materials previously listed the vendored PKI bundle only by its aggregate name, so a vulnerability scanner could not map an advisory against the specific packages it contains. The SBOM now enumerates those nested packages — each with its own version, package URL, and dependency edge — so a scan keys on the version actually shipped. The nested versions ride the same drift-gated projection as the rest of the vendored surface, so they cannot silently go stale on a vendor refresh. No operator action is required.",
  "sections": [
    {
      "heading": "Supply chain",
      "items": [
        {
          "title": "Nested packages of a vendored meta-bundle are enumerated in the SBOM",
          "body": "The vendored PKI bundle is a meta-bundle that ships several packages under one aggregate name. The release software bill of materials previously recorded only that aggregate, so a CVE/SBOM scanner could not match an advisory against the nested packages it actually contains. Each nested package is now emitted as its own SBOM component — with its own version, package URL, and a dependency edge from the bundle — and those nested versions are carried by the drift-gated transitive projection (populated at the vendoring step, verified in release preparation and continuous integration), so a vendor refresh can no longer leave a stale nested version in the bill of materials. The SBOM is the only place these versions appear; no client behaviour or runtime surface changes."
        }
      ]
    }
  ]
}
