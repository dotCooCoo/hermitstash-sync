{
  "version": "0.9.13",
  "date": "2026-06-24",
  "headline": "Refreshes the vendored framework and hardens server-URL display, the WebSocket control channel, log reads, and the encrypted request path",
  "summary": "This release refreshes the vendored framework to v0.15.19 and tightens a broad set of client behaviours. The commands that display the configured server URL now validate and mask it, so a value carrying embedded credentials is refused rather than printed, and only the normalized scheme and host are shown. A permanently-failing WebSocket handshake now fails closed with an actionable message instead of reconnecting forever or sitting silently in a reconnecting state. The local log and metrics reads refuse a symlinked path, and an encrypted-route request now enforces an overall wall-clock timeout that tears the connection down rather than leaving a stalled socket open. It also corrects several edge cases: a blank boolean environment variable no longer overrides an explicit configuration value, the sync cursor is hardened against a corrupt stored value, the support bundle again reports an absent configuration correctly, and a buffered local delete targets the current server file. No configuration changes are required for the common case; see Migration for the one boolean-environment-variable change.",
  "sections": [
    {
      "heading": "Changed",
      "items": [
        {
          "title": "A blank boolean environment variable is treated as unset",
          "body": "A boolean tunable such as HERMITSTASH_AUTO_UPDATE set to an empty or whitespace-only value is now treated as unset and preserves the value from config.json, matching how the numeric and list environment variables already behave. Previously a blank value was read as true and could silently re-enable a setting an operator had turned off in config.json. Set the variable to an explicit true/false (or 1/0) to override the configured value."
        }
      ]
    },
    {
      "heading": "Fixed",
      "items": [
        {
          "title": "A permanently-misconfigured WebSocket target stops reconnecting forever",
          "body": "When the control-channel dial failed for a config-time reason (a malformed server URL or a rejected handshake), the daemon would redial the same hopeless target indefinitely on the backoff ladder with no operator signal. Such a failure now fails closed with an actionable message — fix the server URL and restart — instead of churning."
        },
        {
          "title": "A terminal WebSocket handshake no longer leaves the engine stuck reconnecting",
          "body": "For a handshake that failed terminally without an HTTP status (a non-WebSocket peer or a malformed upgrade response), the engine could report a reconnecting state forever while the control channel was in fact dead and would never redial. Those failures now surface on the same fail-closed path as a rejected handshake, so the daemon reports a clear error to restart rather than a silent, never-progressing reconnect."
        },
        {
          "title": "The sync cursor is hardened against a corrupt stored value",
          "body": "A corrupt or non-numeric stored sync position could read back as Not-a-Number, which stranded the cursor so it never advanced and every reconnect re-ran a full catch-up. The cursor now reads back as a finite, non-negative integer (falling back to zero), and a non-finite position is refused on write so a transient bad value cannot become a permanent stuck cursor."
        },
        {
          "title": "The catch-up complete signal is reset on reconnect",
          "body": "A reconnect that re-entered catch-up could carry over a stale catch-up-complete latch from the previous session and briefly report the daemon as synced while it was still draining the catch-up batch. The latch is now reset on every reconnect, so the synced state is reported only after the new catch-up genuinely completes."
        },
        {
          "title": "A buffered local delete targets the current server file",
          "body": "A local delete is buffered briefly to detect a rename. The buffered delete now re-reads the tracked record when it fires rather than acting on the snapshot captured a moment earlier, so a server-side change to the same path within that window can no longer cause the wrong server file to be deleted."
        },
        {
          "title": "The support bundle reports an absent configuration correctly",
          "body": "After the framework refresh changed the shape of the file-not-found error from the bounded file reader, the support bundle treated a genuinely-absent config.json as unreadable instead of absent. The absent case is now recognized through the reader's typed error hook, so the bundle reports it accurately."
        }
      ]
    },
    {
      "heading": "Security",
      "items": [
        {
          "title": "Server-URL display validates and masks the configured value",
          "body": "Every command that prints the configured server URL — status, repair, and the environment-driven init — now routes it through the same validator the sync and WebSocket transports use and shows only the normalized scheme and host. A URL carrying embedded user:pass credentials is refused outright rather than echoed, so a credential can never reach a status line or log."
        },
        {
          "title": "Log and metrics reads refuse a symlinked path",
          "body": "Reading the daemon log (including the follow mode) and the metrics sidecar now opens the file with a symlink-refusing read, so a symlink planted at the log or sidecar path is refused rather than followed to read an attacker-chosen file through the operator's own privileges. The active log directory is already owner-only; this closes the read side as well."
        },
        {
          "title": "Encrypted-route requests enforce an overall wall-clock timeout",
          "body": "An encrypted metadata request (bundle init, finalize, and rename) now passes an overall wall-clock timeout to the transport, which aborts the underlying connection on expiry. Previously only a zero-progress idle bound was enforced, so a peer that trickled bytes within the idle window could hold the connection open and tie up a connection-pool slot; the connection is now torn down at the ceiling."
        },
        {
          "title": "A keychain store failure surfaces before the plaintext fallback",
          "body": "When the OS keychain is present but the store fails (for example a locked login keychain or an unavailable session bus), the client now logs one actionable line — naming the fallback file and how to migrate the secret back — before writing the API key to the owner-only fallback file. The line carries no key material. A genuinely-absent keychain still falls back silently, as before."
        }
      ]
    },
    {
      "heading": "Supply chain",
      "items": [
        {
          "title": "Vendored framework refreshed to v0.15.19",
          "body": "The single vendored upstream is updated to v0.15.19, which restores stapled-OCSP freshness enforcement, classifies a self-update signature by its structure (failing closed on an indeterminate shape), bounds outbound clients with an overall wall-clock timeout, and refuses a symlinked append-only log path atomically at open time. The integrity manifest is recomputed against the refreshed bytes."
        },
        {
          "title": "The release SBOM's transitive surface is a drift-gated projection",
          "body": "The transitive components in the release software bill of materials are now a mechanical projection of the framework's own vendored manifest, recorded in the committed manifest at the vendoring step and verified by a drift gate in the release preparation and continuous-integration checks. A change to a transitive bundle can no longer reach the published SBOM without a reviewed, gated update."
        }
      ]
    },
    {
      "heading": "Migration",
      "items": [
        {
          "title": "A blank boolean environment variable no longer overrides configuration",
          "body": "If a deployment relies on setting a boolean environment variable to an empty value to force a setting on, that no longer takes effect — a blank value is now treated as unset and the config.json value applies. Set the variable to an explicit true/false (or 1/0) to override. Deployments that set these variables to real values, or do not set them at all, are unaffected."
        }
      ]
    }
  ]
}
