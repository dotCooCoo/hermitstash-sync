{
  "version": "1.0.10",
  "date": "2026-08-09",
  "headline": "Refresh the vendored framework and keep self-update within its rewritten pattern screen",
  "summary": "The vendored framework moves to 0.18.18, which rewrites the screen every operator-supplied pattern is checked against before it is allowed to run. The pattern this client matches release assets with is no longer accepted in the form it was written, so it has been respelled — without that, self-update refuses to start.",
  "sections": [
    {
      "heading": "Fixed",
      "items": [
        {
          "title": "The release-asset matcher is accepted by the framework's pattern screen again",
          "body": "Self-update screens its asset and signature patterns for catastrophic backtracking before running them, and refuses to construct if either is rejected. The optional prerelease and build-metadata tail was written as a bare bounded character class, which the rewritten screen declines because the class also matches the platform suffix that follows it, leaving the boundary between them undecided. The tail now leads with its single separator character, which fixes where it starts. Every name that matched before still matches — stable, prerelease, build metadata, and both at once — and the wrong-platform, wrong-architecture and ML-DSA sidecar names are still excluded."
        }
      ]
    },
    {
      "heading": "Changed",
      "items": [
        {
          "title": "The vendored framework moves to 0.18.18",
          "body": "A handshake the outbound posture refuses now reports which clause refused it and against which peer, instead of surfacing a bare alert that named neither and was routinely read as the wrong cause. The post-quantum primitives behind ML-KEM, ML-DSA and SLH-DSA refresh to @noble/post-quantum 0.7.0, an arithmetic and allocation rework whose outputs were compared byte-for-byte against the previous release; the certificate toolkit moves to @blamejs/pki 0.4.11."
        },
        {
          "title": "The TLS group preference is configured in one place",
          "body": "Each outbound agent set the preference twice: once through the option the TLS layer reads, and again by assigning it to a second name afterwards. That second name is not a TLS option — a value assigned to it is accepted and discarded — so it never carried the preference, and its presence suggested a second handle that could be narrowed when narrowing it would have changed nothing. Only the option the TLS layer actually reads is set now. The groups offered on the wire are unchanged."
        }
      ]
    }
  ]
}
