{
  "version": "0.9.14",
  "date": "2026-06-25",
  "headline": "Refreshes the vendored framework to v0.15.20 for accurate SBOM scanner metadata, and restores the container packaging smoke-test",
  "summary": "This release refreshes the vendored framework to v0.15.20, which corrects the version and CPE metadata a CVE/SBOM scanner reads for the vendored cryptographic and supporting bundles so an advisory is matched against the version actually shipped, and refreshes the bundled WebAuthn and public-suffix data. The client's runtime is unchanged — every file the client consumes is byte-identical to the previous release. It also restores the container packaging smoke-test, which had started failing on a stale match after the previous release normalized the displayed server URL, and removes two unused local variables. No operator action is required.",
  "sections": [
    {
      "heading": "Fixed",
      "items": [
        {
          "title": "Container packaging smoke-test matches the normalized enrollment line",
          "body": "The release smoke-test that verifies the container binary loads and reaches the enrollment step matched only the older trailing-slash form of the displayed server URL. Now that the server URL is normalized to its scheme and host before display, the smoke-test accepts the normalized line, so the container release is validated again. The published image itself was unaffected and the binary was always functional."
        }
      ]
    },
    {
      "heading": "Changed",
      "items": [
        {
          "title": "Remove two unused local variables",
          "body": "Two computed-but-unused local variables — one in the status display, one in the release SBOM projection helper — are removed. This is an internal cleanup with no change to behaviour or output."
        }
      ]
    },
    {
      "heading": "Supply chain",
      "items": [
        {
          "title": "Vendored framework refreshed to v0.15.20",
          "body": "The single vendored upstream is updated to v0.15.20, which corrects the structured component versions and CPE strings the vendored manifest records for a CVE/SBOM scanner (a vendored elliptic-curve bundle's CPE and a PKI bundle's component version had drifted from the code actually shipped, which could match an advisory against the wrong version) and derives them from the installed package versions so they cannot drift again. It also refreshes the bundled WebAuthn server library and the Mozilla Public Suffix List. Every file this client consumes at runtime is byte-identical to the previous release; the corrected metadata flows into the release software bill of materials through the drift-gated transitive projection, so a vulnerability scan now keys on the shipped versions. The integrity manifest is recomputed against the refreshed bytes."
        }
      ]
    }
  ]
}
