{
  "minor": "0.8",
  "releases": [
    {
      "version": "0.8.25",
      "date": "2026-05-31",
      "headline": "Post-quantum enrollment fix, sync and transfer hardening, blamejs v0.14.10",
      "summary": "Restores the post-quantum TLS posture on the enrollment handshake, stops the sync loop from stalling after a rejected server event, hardens HTTP-client shutdown and interrupted transfers, reclaims orphaned download temp files from nested directories, and advances the vendored framework to v0.14.10.",
      "sections": [
        {
          "heading": "Security",
          "items": [
            {
              "title": "Enrollment handshake advertises the post-quantum TLS posture",
              "body": "First-contact enrollment negotiated Node's default elliptic curves over TLS 1.2 instead of the post-quantum group preference every other request uses, because it built a bare HTTPS agent that bypassed the transport's posture injection. Against a server that enforces the post-quantum gate, enrollment was rejected at the TLS handshake. The enrollment client now offers the same ML-KEM hybrid groups and pins TLS 1.3, so enrollment succeeds against a hardened server; self-signed certificates are still accepted at first contact, with the server-issued client certificate and CA pin taking over immediately after."
            }
          ]
        },
        {
          "heading": "Fixed",
          "items": [
            {
              "title": "Sync no longer stalls after a rejected server event",
              "body": "If a server change event at the tip of the change log was rejected by the client's path-traversal guard, the sync cursor stopped advancing, and every reconnect replayed the same blocked event ahead of newer ones — silently halting further sync. Rejected events now advance the cursor so later changes continue to apply and catch-up completes."
            },
            {
              "title": "Hardened HTTP-client shutdown and interrupted transfers",
              "body": "Three lifecycle defects are fixed: a buffered rename-detection delete could fire after shutdown against a torn-down client and surface as an unhandled rejection; an aborted download response left its file handle open and its partial temp file behind; and reloading client certificates after a CA rotation aborted any in-flight upload or download. Pending deletes are now cancelled on shutdown, an interrupted download cleans up and rejects, and the previous connection pool drains across a credential swap instead of being destroyed mid-transfer."
            },
            {
              "title": "Interrupted-download temp files reclaimed from subdirectories",
              "body": "The startup sweep that removes leftover download temp files only scanned the sync-folder root, so a temp left inside a nested directory accumulated indefinitely. The sweep now recurses into subdirectories and matches only the downloader's exact temp-file suffix, so a user file that merely contains that token in its name is never removed."
            },
            {
              "title": "Failure messages from newer servers surface correctly",
              "body": "Enrollment and certificate-renewal failures now read the server's structured error detail field, falling back to the legacy field, so the message shown to the operator matches what a current server returns instead of a generic status line."
            }
          ]
        },
        {
          "heading": "Supply chain",
          "items": [
            {
              "title": "Vendored blamejs refreshed to v0.14.10",
              "body": "The bundled framework advances from v0.11.33 to v0.14.10. The wire-layer surfaces this client consumes — the WebSocket client, the HTTP client and its per-session encryption envelope, the post-quantum TLS agent, and the JSON parser — carry no API or behaviour change across the gap, and the vendored cryptographic bundles are byte-identical, so the runtime behaviour of the binary is unchanged. The release SBOM gains the @noble/curves bundle the framework now ships."
            }
          ]
        }
      ]
    },
    {
      "version": "0.8.24",
      "date": "2026-05-21",
      "headline": "Vendored blamejs refreshed to v0.11.33 plus public release history at `CHANGELOG.md`",
      "summary": "Tooling and docs release: the bundled framework advances to v0.11.33 (the wire-layer surfaces this client consumes are byte-identical to v0.11.32), and the full release history from v0.1.0 onward is now committed under `release-notes/` as structured JSON with a regenerated `CHANGELOG.md` at the repo root.",
      "sections": [
        {
          "heading": "Added",
          "items": [
            {
              "title": "Public release history at `CHANGELOG.md` and `release-notes/`",
              "body": "Every release from v0.1.0 forward is now committed as structured JSON under `release-notes/` — per-patch files for the current minor line, consolidated `vX.Y.x.json` for historical minor lines. `CHANGELOG.md` at the repo root is generated from the JSON tree via `node scripts/generate-changelog-entry.js --rebuild` and groups entries by minor with newest first. Downstream automation can consume the JSON tree without parsing markdown."
            },
            {
              "title": "Maintainer release flow documented at `RELEASING.md`",
              "body": "A new `RELEASING.md` at the repo root walks the maintainer side of cutting a release end-to-end: authoring the release-notes JSON, the pre-tag local gates, the signed-tag step, the per-job artifact breakdown across `release.yml`, the required GitHub Actions secrets, and the ML-DSA-65 release-signing-key setup and rotation flow."
            }
          ]
        },
        {
          "heading": "Changed",
          "items": [
            {
              "title": "Vendored blamejs refreshed to v0.11.33",
              "body": "`vendor/MANIFEST.json` advances from v0.11.32 to v0.11.33. Every tracked file under `vendor/blamejs/` is byte-identical between the two versions — the upstream changes land in surfaces this client does not consume — so the runtime behaviour of the SEA binary is unchanged."
            }
          ]
        }
      ]
    },
    {
      "version": "0.8.23",
      "date": "2026-05-21",
      "headline": "SLSA L3 provenance, cosign-signed SBOM, ML-DSA-65 sidecar per binary",
      "summary": "Every release now ships a SLSA L3 provenance attestation, a Sigstore-keyless cosign signature over the SBOM (and VEX, when present), and an ML-DSA-65 (FIPS 204) signature sidecar per platform binary for operators with a post-quantum verification posture. The existing GPG, P-384 ECDSA, SHA-256, and SHA3-512 artifacts continue to ship unchanged.",
      "sections": [
        {
          "heading": "Added",
          "items": [
            {
              "title": "Structured `CHANGELOG.md` driven by `release-notes/v<version>.json`",
              "body": "Every release now lives as a structured JSON entry under `release-notes/`. The workflow renders the GitHub-release-page markdown directly from the JSON, and `CHANGELOG.md` is generated from the full tree via `node scripts/generate-changelog-entry.js --rebuild`. The JSON shape, validator, section-allowlist ordering, and rollup mechanics mirror the upstream blamejs framework's release-notes pipeline."
            }
          ]
        },
        {
          "heading": "Changed",
          "items": [
            {
              "title": "Vendored blamejs refreshed to v0.11.32",
              "body": "The pinned upstream version in `vendor/blamejs/` (and `vendor/MANIFEST.json`) advances from v0.11.2 to v0.11.32. The wire-layer surfaces the sync client consumes (`b.wsClient`, `b.httpClient`, `b.pqcAgent`, `b.safeJson`, `b.atomicFile`, `b.retry`, `b.appShutdown`, `b.middleware.apiEncrypt`, ML-KEM-1024 + XChaCha20-Poly1305 envelopes) are byte-identical between the two versions; the refresh picks up upstream additions and security fixes in adjacent modules."
            }
          ]
        },
        {
          "heading": "Supply chain",
          "items": [
            {
              "title": "SLSA L3 provenance attestation per release",
              "body": "`hermitstash-sync-vX.Y.Z.intoto.jsonl` is now attached to every release, produced by the `slsa-framework/slsa-github-generator` reusable workflow and signed via Sigstore-keyless. The attestation covers every platform binary plus the SBOM (and VEX when present); operators verify the binary they downloaded was built by this repo's `release.yml` at the matching commit and tag via `slsa-verifier verify-artifact <binary> --provenance-path <jsonl> --source-uri github.com/dotCooCoo/hermitstash-sync --source-tag vX.Y.Z`."
            },
            {
              "title": "Sigstore-keyless cosign signature over the SBOM (and VEX)",
              "body": "`*.cdx.json.sigstore` and (when emitted) `*.vex.json.sigstore` are now attached to every release. Each is a cosign sign-blob bundle binding the SBOM or VEX bytes to the workflow's OIDC identity in the Sigstore transparency log. Verify with `cosign verify-blob --bundle <bundle>.sigstore --certificate-identity-regexp 'https://github.com/dotCooCoo/hermitstash-sync/' --certificate-oidc-issuer 'https://token.actions.githubusercontent.com' <target>`."
            },
            {
              "title": "ML-DSA-65 release-signing sidecar (FIPS 204)",
              "body": "Every platform binary ships an additional `<binary>.mldsa.sig` raw signature alongside the existing P-384 ECDSA `.sig` and armored GPG `.asc`. The release-signing pubkey lives in-tree at `keys/release-pqc-pub.json`; its SHA3-512 fingerprint is recorded in `SECURITY.md` for out-of-band verification. The daemon's auto-update path continues to verify against the embedded P-384 ECDSA pubkey via `node:crypto`; the ML-DSA-65 sidecar is additive for operators with a PQC-only verification posture."
            },
            {
              "title": "Tag-matches-VERSION gate in the release workflow",
              "body": "`.github/workflows/release.yml` now fails the matrix build immediately if the pushed `vX.Y.Z` tag does not match the `VERSION` constant compiled into the SEA binary. A mismatched tag previously produced a binary that reported a different version than the asset name it shipped under."
            }
          ]
        }
      ]
    },
    {
      "version": "0.8.22",
      "date": "2026-05-19",
      "headline": "Resolve SEA SIGTERM exit-code handling under systemd",
      "summary": "Closes out the SIGTERM-exit-code work begun in v0.8.17. The hardened systemd unit now treats Node's SIGTERM-exit-143 from a SEA binary as a clean shutdown via `SuccessExitStatus=143`, so `systemctl stop` no longer flags the daemon as failed.",
      "sections": [
        {
          "heading": "Fixed",
          "items": [
            {
              "title": "`SuccessExitStatus=143` whitelisted in `deploy/hermitstash-sync.service`",
              "body": "Under SEA, Node currently exits with code 143 on a clean SIGTERM shutdown rather than the conventional zero. The shipped systemd unit now treats 143 as success so `systemctl stop hermitstash-sync` reports `inactive (dead)` instead of `failed`, and the external auto-update timer no longer fights a phantom restart loop. The in-binary SEA-SIGTERM detector logs the remap path so operators on bare-binary installs can see why exit 143 appears in their journals."
            }
          ]
        }
      ]
    },
    {
      "version": "0.8.21",
      "date": "2026-05-19",
      "headline": "SIGTERM wall-clock cap + SEA-SIGTERM detector",
      "summary": "Intermediate step in the SEA SIGTERM exit-code work resolved in v0.8.22.",
      "sections": [
        {
          "heading": "Fixed",
          "items": [
            {
              "title": "SIGTERM shutdown bounded by a wall-clock cap",
              "body": "The daemon's SIGTERM teardown now hard-stops at a wall-clock budget so a hung phase can't pin the process forever. A SEA-SIGTERM detector annotates exit-code 143 traces for downstream supervisors. Final resolution under systemd lands in v0.8.22 (`SuccessExitStatus=143`)."
            }
          ]
        }
      ]
    },
    {
      "version": "0.8.20",
      "date": "2026-05-19",
      "headline": "Remap SEA Node SIGTERM exit code via `tini -e 143`",
      "sections": [
        {
          "heading": "Fixed",
          "items": [
            {
              "title": "Container entrypoint exits zero on clean SIGTERM",
              "body": "The container image's `tini` init now passes `-e 143` so a SEA-Node SIGTERM (exit 143) is remapped to a clean zero at the container boundary. Diagnostic step toward the systemd-side resolution in v0.8.22."
            }
          ]
        }
      ]
    },
    {
      "version": "0.8.19",
      "date": "2026-05-19",
      "headline": "Minimal SIGTERM handler to isolate SEA exit-code behavior",
      "sections": [
        {
          "heading": "Fixed",
          "items": [
            {
              "title": "Stripped-down SIGTERM handler isolates the SEA exit-code path",
              "body": "Diagnostic release that reduces the SIGTERM handler to a minimal shape so the SEA-Node exit-code behavior can be characterized without surrounding teardown noise. Superseded by the full fix in v0.8.22."
            }
          ]
        }
      ]
    },
    {
      "version": "0.8.18",
      "date": "2026-05-19",
      "headline": "Instrument SIGTERM shutdown trace",
      "sections": [
        {
          "heading": "Fixed",
          "items": [
            {
              "title": "Structured trace emitted across the SIGTERM teardown path",
              "body": "Adds per-phase log lines covering shutdown to surface where exit-code 143 originates under SEA. Diagnostic only; the user-visible resolution lands in v0.8.22."
            }
          ]
        }
      ]
    },
    {
      "version": "0.8.17",
      "date": "2026-05-19",
      "headline": "Own SIGTERM/SIGINT handlers with explicit `process.exit`",
      "summary": "Replaces the framework-default signal disposition with HermitStash-owned handlers that call `process.exit` after the daemon's shutdown phases complete. Surfaces a SEA-specific exit-code-143 quirk that the v0.8.18 through v0.8.22 series then resolves.",
      "sections": [
        {
          "heading": "Fixed",
          "items": [
            {
              "title": "Deterministic shutdown via explicit `process.exit` post-teardown",
              "body": "`lib/daemon.js` now installs its own `SIGTERM`/`SIGINT` handlers that run the `b.appShutdown` phases and then call `process.exit(0)` explicitly. Previously a stray timer or socket could keep the event loop alive after shutdown, leaving the process pinned. Operators on the SEA build see exit code 143 on clean shutdown — that quirk is addressed at the systemd-unit + container-init layer in v0.8.20 and v0.8.22."
            }
          ]
        }
      ]
    },
    {
      "version": "0.8.16",
      "date": "2026-05-19",
      "headline": "Vendor blamejs v0.11.2 + synchronous SIGTERM exit",
      "sections": [
        {
          "heading": "Changed",
          "items": [
            {
              "title": "Vendored blamejs refreshed to v0.11.2",
              "body": "Re-clones `vendor/blamejs/` at v0.11.2 and updates `vendor/MANIFEST.json`. Picks up upstream wire-layer hardening and adjacent-module security fixes; the wrapper surfaces consumed by this client are unchanged."
            }
          ]
        },
        {
          "heading": "Fixed",
          "items": [
            {
              "title": "Sync engine teardown exits synchronously on SIGTERM",
              "body": "Drops the `setImmediate` deferral on the sync engine's SIGTERM teardown path so the process can exit on the same tick once shutdown phases complete. The deferral was occasionally racing with a Node-internal close that would observe a still-alive engine and trap the process."
            }
          ]
        }
      ]
    },
    {
      "version": "0.8.15",
      "date": "2026-05-19",
      "headline": "Per-instance test CA + deterministic SIGTERM container exit",
      "sections": [
        {
          "heading": "Fixed",
          "items": [
            {
              "title": "Test harness generates a per-instance CA",
              "body": "The test suite previously reused a shared self-signed CA across runs, which made cert-rotation tests flaky against parallel runners. Each test instance now mints its own CA + server cert + client cert at startup, isolating runs from each other."
            },
            {
              "title": "Container SIGTERM exit is deterministic",
              "body": "Aligns the daemon's signal handling with the container init so that `docker stop`/`podman stop` reliably produces the same exit semantics each shutdown. Removes a flaky race that occasionally left the container reporting OOMKilled-style codes after a clean stop signal."
            }
          ]
        }
      ]
    },
    {
      "version": "0.8.14",
      "date": "2026-05-19",
      "headline": "Vendor blamejs v0.10.14",
      "sections": [
        {
          "heading": "Changed",
          "items": [
            {
              "title": "Vendored blamejs refreshed to v0.10.14",
              "body": "Re-clones `vendor/blamejs/` at v0.10.14 and bumps `vendor/MANIFEST.json` to match. Picks up upstream-side fixes in modules adjacent to the wire-layer surfaces this client consumes; the wrapper API is unchanged."
            }
          ]
        }
      ]
    },
    {
      "version": "0.8.13",
      "date": "2026-05-19",
      "headline": "Periodic cert check, config validation, env overlay, watchdogs",
      "summary": "Addresses a batch of findings against the daemon's long-running posture: certificate validity is checked on a schedule rather than only at startup, `config.json` is schema-validated before the daemon accepts it, environment variables can overlay individual config keys, and the sync + websocket loops are now externally watchdog-ed.",
      "sections": [
        {
          "heading": "Added",
          "items": [
            {
              "title": "Periodic certificate validity check",
              "body": "`lib/cli.js` now runs the mTLS cert validity check on a recurring schedule against the operator clock, not only at process start. A cert that crosses the 60-day renewal threshold during a long-running daemon now triggers auto-renew without requiring a restart."
            },
            {
              "title": "Schema validation of `config.json` at load time",
              "body": "`lib/config.js` now refuses a config file whose shape, types, or value ranges don't match the documented schema. Operators get a precise error line + the field name that failed validation instead of an opaque runtime crash later."
            },
            {
              "title": "Environment-variable overlay for individual config keys",
              "body": "Selected `config.json` keys can now be overridden via `HERMITSTASH_*` environment variables, which is the conventional shape for containerized + systemd-unit deployments where editing the on-disk config isn't ergonomic. Documented in the README's configuration section."
            },
            {
              "title": "Watchdog timers on the sync + websocket loops",
              "body": "Both core loops now check in against a watchdog with a generous slack budget; missing a check-in logs a structured warning and forces a re-init of the affected loop. A wedged loop no longer silently stops making progress."
            },
            {
              "title": "Unit tests for the new validators + overlays",
              "body": "`tests/` gains coverage for the config schema validator, the env-overlay merge rules, and the cert-check scheduling behavior. Runs alongside the existing E2E + integration suite via `tests/run-all.js`."
            }
          ]
        }
      ]
    },
    {
      "version": "0.8.12",
      "date": "2026-05-18",
      "headline": "Vendor blamejs v0.10.13 + drop locals now upstream",
      "sections": [
        {
          "heading": "Changed",
          "items": [
            {
              "title": "Vendored blamejs refreshed to v0.10.13",
              "body": "Re-clones `vendor/blamejs/` at v0.10.13 and updates `vendor/MANIFEST.json`. Local copies of helpers that have since landed upstream are dropped in favor of the framework versions, reducing the divergence surface."
            },
            {
              "title": "README documents SPKI pinning + Prometheus sidecar + historical-stats framing",
              "body": "Adds operator-facing documentation for the v0.8.11 `pinnedServerSpki` config option, the v0.8.10 Prometheus sidecar exposition, and the framing of historical vs live stats reported by the `stats` CLI subcommand."
            }
          ]
        }
      ]
    },
    {
      "version": "0.8.11",
      "date": "2026-05-18",
      "headline": "Opt-in SPKI certificate pinning",
      "summary": "Adds a `pinnedServerSpki` config option that pins the server's SPKI hash on top of the existing PQC-mTLS posture. With pinning enabled, a CA compromise that issues a rogue cert for the configured server host is detected before the TLS handshake completes.",
      "sections": [
        {
          "heading": "Added",
          "items": [
            {
              "title": "`pinnedServerSpki` config option",
              "body": "Operators can pin the SHA-256 of the server's SubjectPublicKeyInfo by setting `pinnedServerSpki` in `config.json`. On every outbound HTTPS / WebSocket connection the client computes the SPKI hash of the presented leaf cert and refuses the connection if it doesn't match the pin. Compatible with the PQC-mTLS handshake unchanged; the pin is checked after the regular CA validation passes. Set the value to the empty string or omit it to keep the existing CA-validation-only behavior."
            }
          ]
        }
      ]
    },
    {
      "version": "0.8.10",
      "date": "2026-05-18",
      "headline": "Upload + download latency histograms with Prometheus sidecar",
      "summary": "The daemon now records per-operation latency histograms for both uploads and downloads, and exposes them via an optional sidecar HTTP endpoint in Prometheus exposition format. Existing JSON-stats output from the `stats` CLI is unaffected.",
      "sections": [
        {
          "heading": "Added",
          "items": [
            {
              "title": "Latency histograms for uploads + downloads",
              "body": "Both transfer paths now feed a fixed-bucket histogram so operators can scrape p50 / p95 / p99 from a single `stats` snapshot. Buckets follow Prometheus conventions and span sub-millisecond through tens-of-seconds latencies."
            },
            {
              "title": "Optional Prometheus sidecar exposition endpoint",
              "body": "When enabled via config, the daemon binds an HTTP listener on a loopback-only port and serves `/metrics` in Prometheus exposition format. Disabled by default; bind address, port, and authentication mode are all configurable. The existing JSON-stats output via the `stats` CLI subcommand continues to work unchanged."
            }
          ]
        }
      ]
    },
    {
      "version": "0.8.9",
      "date": "2026-05-18",
      "headline": "Windows `start --daemon` survives parent exit",
      "sections": [
        {
          "heading": "Fixed",
          "items": [
            {
              "title": "Detached daemon survives parent shell exit on Windows",
              "body": "`hermitstash-sync start --daemon` on Windows previously inherited the parent console's process group and was killed when the spawning shell closed. The launcher now creates a detached process group via `detached: true` + `stdio: 'ignore'` + `child.unref()` so the daemon outlives the parent shell, matching the Linux / macOS behavior."
            },
            {
              "title": "Clearer `stats` staleness reporting",
              "body": "The `stats` CLI subcommand previously reported stale-data warnings using an opaque timestamp; the new framing surfaces \"last successful sync N seconds ago\" alongside the data so an operator can tell at a glance whether the numbers reflect the current run."
            }
          ]
        }
      ]
    },
    {
      "version": "0.8.8",
      "date": "2026-05-18",
      "headline": "Pick up `shareId` from `/sync/enroll` response",
      "sections": [
        {
          "heading": "Fixed",
          "items": [
            {
              "title": "Enrollment response's `shareId` is now persisted",
              "body": "The `/sync/enroll` response carries the bundle's `shareId`, which the client previously discarded and re-derived later via an extra GET. The client now persists the `shareId` directly out of the enrollment response and the redundant lookup is gone. No on-wire change; reduces one round-trip per first-run enrollment."
            }
          ]
        }
      ]
    },
    {
      "version": "0.8.7",
      "date": "2026-05-18",
      "headline": "TLS CA bundle extends the system trust store",
      "sections": [
        {
          "heading": "Fixed",
          "items": [
            {
              "title": "Configured CA bundle no longer replaces the system trust store",
              "body": "A configured `caBundle` previously overrode Node's built-in CA list entirely, which broke outbound TLS to anything outside the configured CA (notably the GitHub-release downloader used by the auto-updater). The bundle now extends `tls.rootCertificates` rather than replacing it. Operators who explicitly want the override-only behavior can opt in via the new `caBundleMode: 'replace'` config key."
            }
          ]
        }
      ]
    },
    {
      "version": "0.8.6",
      "date": "2026-05-18",
      "headline": "Initial-sync NPE on null bundle metadata + `ca.crt` trailing newline",
      "sections": [
        {
          "heading": "Fixed",
          "items": [
            {
              "title": "Initial sync no longer crashes when the bundle has no metadata yet",
              "body": "A freshly-enrolled bundle with zero files returned a null `metadata` payload from the server, which the client previously dereferenced unconditionally during the initial-sync diff. The diff now treats the null payload as an empty bundle and proceeds, matching the server's intent."
            },
            {
              "title": "Enrolled `ca.crt` written with a trailing newline",
              "body": "Some TLS stacks (notably the macOS Keychain Access import flow) refuse a PEM file without a trailing newline. The enrollment write path now ensures the file ends with `\\n`."
            }
          ]
        }
      ]
    },
    {
      "version": "0.8.5",
      "date": "2026-05-18",
      "headline": "Fix bandwidth limiter double-spend under concurrent uploads",
      "sections": [
        {
          "heading": "Fixed",
          "items": [
            {
              "title": "Token-bucket increment under parallel uploads",
              "body": "The bandwidth limiter introduced in v0.8.3 used a non-atomic check-then-decrement against the token bucket; under the parallel-upload concurrency added in v0.8.2 two uploads could spend the same token, causing the limiter to overshoot the configured cap by up to `concurrency - 1` tokens per refill window. The bucket update is now serialized through the limiter's own queue so each token is only spent once."
            }
          ]
        }
      ]
    },
    {
      "version": "0.8.4",
      "date": "2026-05-17",
      "headline": "Windows long-path + SIGHUP config reload + beta auto-update channel",
      "sections": [
        {
          "heading": "Added",
          "items": [
            {
              "title": "Windows long-path (`\\\\?\\`) support across the sync path",
              "body": "Every filesystem-touching call in `lib/watcher.js`, `lib/sync-engine.js`, and `lib/checksum.js` now opts into the Windows long-path API so paths beyond the legacy MAX_PATH (260 chars) no longer truncate or fail with `ENOENT`. Behavior on Linux / macOS is unchanged."
            },
            {
              "title": "`SIGHUP` reloads `config.json` in-place",
              "body": "The daemon's `SIGHUP` handler now re-reads `config.json` and applies non-structural changes (sync paths, ignore patterns, bandwidth caps, log level) without a restart. Structural changes that require a re-handshake (server URL, mTLS cert/key) still need a full restart; the handler logs which keys were re-applied vs ignored."
            },
            {
              "title": "Opt-in `beta` auto-update channel",
              "body": "`autoUpdateChannel: 'beta'` in `config.json` (or `--channel=beta` on the CLI) makes the updater include pre-release tags when polling for the next version. Default channel remains `stable` (release-tag-only)."
            }
          ]
        }
      ]
    },
    {
      "version": "0.8.3",
      "date": "2026-05-17",
      "headline": "Selective sync + bandwidth limit",
      "sections": [
        {
          "heading": "Added",
          "items": [
            {
              "title": "Subdirectory allowlist for selective sync",
              "body": "`config.json` now accepts an `includePaths` array of subdirectories relative to the sync root. When set, only files under the listed prefixes are watched, uploaded, and accepted from inbound websocket events. Paths outside the allowlist are ignored entirely. Combine with the existing `ignorePatterns` for fine-grained selection."
            },
            {
              "title": "Outbound bandwidth limit (token-bucket)",
              "body": "`config.json#bandwidthLimitKbps` caps the upload throughput at the configured kilobits per second via a token-bucket limiter on the upload path. Unset or `0` disables the limit and restores the previous full-bandwidth behavior. Download throttling is not implemented in this release."
            }
          ]
        }
      ]
    },
    {
      "version": "0.8.2",
      "date": "2026-05-17",
      "headline": "`diagnose` subcommand + conflict copies + parallel uploads + systemd notify",
      "sections": [
        {
          "heading": "Added",
          "items": [
            {
              "title": "`hermitstash-sync diagnose` subcommand",
              "body": "Runs a one-shot health pass against the configured server: pubkey fetch, mTLS handshake check, bundle metadata round-trip, websocket connect + heartbeat, clock drift vs server time. Prints a structured report and a non-zero exit code on any failed check, suitable for use as a readiness probe."
            },
            {
              "title": "Conflict copies on last-write-wins overwrite",
              "body": "When the last-write-wins resolver overwrites a local edit, the previous local copy is preserved as `<filename>.conflict-<timestamp>` in the same directory rather than being silently lost. The operator can recover or re-merge from the conflict copy at their leisure."
            },
            {
              "title": "Parallel upload pool",
              "body": "Upload concurrency is now configurable via `config.json#uploadConcurrency` (default `4`). Multiple file uploads run in parallel against the server instead of serially, which materially improves first-sync wall-clock time on bundles with many small files."
            },
            {
              "title": "`sd_notify`-style systemd readiness signaling",
              "body": "When launched under systemd with `NOTIFY_SOCKET` set, the daemon now emits `READY=1` after the initial handshake + first-sync completes, and periodic `WATCHDOG=1` heartbeats inside the configured `WatchdogSec` window. `Type=notify` units now report `active (running)` only after the daemon is genuinely ready to handle changes."
            }
          ]
        },
        {
          "heading": "Changed",
          "items": [
            {
              "title": "README requirements line updated for blamejs v0.10.6",
              "body": "The README's runtime-requirements section now lists the vendored blamejs version that v0.8.1 bumped to. Operators reading the README to verify their build no longer see a stale v0.9.54 number."
            }
          ]
        }
      ]
    },
    {
      "version": "0.8.1",
      "date": "2026-05-17",
      "headline": "Vendor blamejs v0.10.6 + CycloneDX 1.6 SBOM on every release",
      "summary": "First release that emits a CycloneDX 1.6 SBOM alongside the binaries and matches the vendored framework to blamejs v0.10.6. Operators on Dependency-Track, OWASP Dependency-Check, Snyk SBOM Monitor, or any CPE-2.3-aware scanner can now map CVE advisories directly against the components shipping inside the SEA binary.",
      "sections": [
        {
          "heading": "Changed",
          "items": [
            {
              "title": "Vendored blamejs refreshed to v0.10.6",
              "body": "Re-clones `vendor/blamejs/` at v0.10.6 and updates `vendor/MANIFEST.json`. Picks up the upstream framework's accumulated security fixes and additions since v0.9.54; the wire-layer wrapper surfaces consumed by this client (`b.wsClient`, `b.httpClient`, `b.pqcAgent`, `b.safeJson`, `b.atomicFile`, `b.retry`, `b.appShutdown`, `b.middleware.apiEncrypt`) are API-stable across the bump."
            }
          ]
        },
        {
          "heading": "Supply chain",
          "items": [
            {
              "title": "CycloneDX 1.6 SBOM emitted on every release",
              "body": "`scripts/build-sbom.js` now runs as part of the `release` job and emits `hermitstash-sync-vX.Y.Z.cdx.json` alongside the platform binaries. The SBOM enumerates the SEA binary's full vendored surface (blamejs + its transitive bundles: noble-ciphers, noble-post-quantum, simplewebauthn-server, common-passwords list, BIMI trust anchors, publicsuffix-list, peculiar-pki) with CPE 2.3 + purl identifiers and per-component SHA-256 hashes. The actions-run URL is recorded in `metadata.lifecycles[].externalReferences[]` for build-provenance back-reference."
            }
          ]
        }
      ]
    },
    {
      "version": "0.8.0",
      "date": "2026-05-16",
      "headline": "Strip internal-rules narrative from operator-facing surface",
      "summary": "Documentation-only minor-line opener. Removes residual references to internal contributor-rules files and tightens before/now narrative that had leaked into README, comments, error messages, and the systemd unit drop-ins. No code or wire-protocol changes; behavior is byte-identical to v0.7.17.",
      "sections": [
        {
          "heading": "Changed",
          "items": [
            {
              "title": "README, inline comments, and error messages reworded to drop internal narrative",
              "body": "Operator-facing prose across `README.md`, public-API JSDoc, CLI `--help` text, error messages thrown from public entry points, and the shipped `deploy/` units no longer references contributor-rules file paths or carries before/now framing that only makes sense with prior-conversation context. Each line now reads as a self-contained statement of what the surface is or does."
            }
          ]
        }
      ]
    }
  ]
}
