{
  "version": "1.0.14",
  "date": "2026-08-19",
  "headline": "Framework currency, a refreshed base image, and a build cache an old tag can no longer write",
  "summary": "A maintenance release. The vendored framework moves forward eight releases, carrying upstream fixes to constant-time comparison and to the parsers that read untrusted input; the container base image is refreshed; and the release workflow no longer lets a rebuild of a superseded tag write the cache every later build reads.",
  "sections": [
    {
      "heading": "Security",
      "items": [
        {
          "title": "Rebuilding an old release tag can no longer write the shared build cache",
          "body": "Both of the Docker workflow's triggers can build an arbitrary existing release tag: the dispatch takes a version from its caller, and the workflow_run trigger fires for a manual dispatch of the release workflow as well as for the tag push. Either route runs that tag's own Dockerfile and install scripts in a job whose cache scope belongs to the default branch, so rebuilding a superseded tag seeded layers built from superseded code into the cache every later build reads. Writing the cache now requires both that the trigger is a workflow_run and that the upstream run came from the tag push. Reading is unchanged, so rebuilding an old tag is still fast and leaves nothing behind."
        },
        {
          "title": "Constant-time comparisons in the framework compare every candidate",
          "body": "Eight places in the vendored framework compared a presented value against a list and stopped at the first match, so the time taken reported the match's position even though each comparison was individually constant-time. Certificate revocation checks, webhook signature verification, HTTP message signatures, attestation nonces, the rotating DPoP nonce, one-time-password drift and breached-password range lookups are all affected."
        },
        {
          "title": "Parsers that read untrusted input carry no pattern literals",
          "body": "The framework's YAML, TOML, INI, .env and XML parsers drop 55 regular expressions, so screening an input costs the length of that input and the byte cap that bounds the input also bounds the screen. A YAML tag beginning with a digit is now refused, as the surrounding rule always described."
        }
      ]
    },
    {
      "heading": "Supply chain",
      "items": [
        {
          "title": "Vendored framework moves to blamejs 0.18.39",
          "body": "Eight releases forward. Beyond the fixes above: a guard now applies the character policy it declares rather than deciding by a finding's severity, the public suffix list ranks a wildcard rule by label count as the specification requires, and citations across the framework move to the documents that replaced RFC 7230-7235, 7489 and 4122. The client keeps pace with the server, which the two have to share for anything crossing the wire."
        },
        {
          "title": "Runtime base image moves to the current build",
          "body": "The pinned Chainguard wolfi-base digest is refreshed. That image is rebuilt whenever upstream fixes land, so a pin left in place falls behind on patches without anything changing here."
        }
      ]
    },
    {
      "heading": "Fixed",
      "items": [
        {
          "title": "The base-image currency check reads each Dockerfile from the path it already resolved",
          "body": "The check rebuilt an absolute path from a relative string on every read rather than using the one its own directory walk already held. Behaviour is unchanged; the reader now opens a path built from directory entries and verifies it stays inside the repository before opening it."
        }
      ]
    }
  ]
}
