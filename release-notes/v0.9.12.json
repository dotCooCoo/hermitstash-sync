{
  "version": "0.9.12",
  "date": "2026-06-21",
  "headline": "Hardens certificate, WebSocket, and credential handling, refreshes the vendored framework, and restores the container image build",
  "summary": "This release refreshes the vendored framework to v0.15.15 and tightens a broad set of client behaviours: a server-pushed certificate rotation now refuses a trust anchor that is not a real CA, the WebSocket control channel fails closed on a classical-TLS downgrade and bounds a fragmented message before it completes, a denied bundle-metadata request surfaces an actionable error instead of syncing nothing, and the on-disk credentials read is symlink-refusing and refuses a world-readable fallback file. It also fixes `log --follow`, lets the beta auto-update channel self-update, restores the container image build that a malformed shell script had skipped, and corrects several sync edge cases. No configuration changes are required for the common case; see Migration for the one credentials-permission change.",
  "sections": [
    {
      "heading": "Security",
      "items": [
        {
          "title": "Certificate rotation enforces the CA basic-constraints bit",
          "body": "When the server pushes a new client certificate, key, and CA, the client now requires the supplied CA to assert basicConstraints cA:TRUE and to have actually issued the new leaf before it adopts the rotated identity. A non-CA end-entity certificate can no longer be accepted as the trust anchor for a swapped-in identity (the classic basic-constraints bypass)."
        },
        {
          "title": "WebSocket classical-TLS downgrade fails closed",
          "body": "If the control-channel handshake negotiates a classical key exchange instead of a post-quantum hybrid, the client now tears the connection down without ever signalling a successful connect, and the normal reconnect path takes over. Previously the engine briefly saw the connection as live before the socket was destroyed. Setting HERMITSTASH_ALLOW_CLASSICAL_TLS=1 still relaxes the floor to a warning."
        },
        {
          "title": "Bundle-metadata request fails closed on a denial",
          "body": "An access-denied or error response to the initial bundle-metadata fetch is now surfaced as an actionable error (and an authentication denial stops the daemon with a re-enroll message) instead of being treated as a successful empty bundle that silently syncs nothing."
        },
        {
          "title": "Credentials file read is symlink-refusing and rejects loose permissions",
          "body": "The plaintext credentials fallback file is read through a symlink-refusing, inode-pinned open, and a group- or world-readable file is now refused with an actionable message rather than being silently re-permissioned. This removes a window where a planted symlink could redirect a permission change at an arbitrary file. See Migration."
        },
        {
          "title": "WebSocket fragmented messages are bounded before completion",
          "body": "Inherited from the framework refresh: an incoming fragmented message is now bounded by its running total as each continuation frame arrives, so a peer can no longer stream unbounded continuation frames to exhaust memory before the message-size cap is checked, and a frame arriving after close is ignored."
        },
        {
          "title": "Diagnostic bundle stops leaking the previous-binary path and bounds log decompression",
          "body": "The support bundle now ships only the basename of the pending-update marker's previous-binary path (it previously embedded the absolute path, exposing the OS username and home-directory layout), and rotated compressed logs are decompressed under an expansion-ratio and output-size guard sized to the configured rotation policy so a crafted log file cannot exhaust memory."
        }
      ]
    },
    {
      "heading": "Fixed",
      "items": [
        {
          "title": "`log --follow` now actually follows",
          "body": "The follow mode exited immediately instead of tailing the log, because its repeating timer did not hold the event loop open. It now stays alive and streams new lines until interrupted."
        },
        {
          "title": "Beta auto-update channel can self-update",
          "body": "The release-asset matcher rejected prerelease version strings (for example a `-beta.N` suffix), so a client on the beta channel could never select a newer beta build. The matcher now accepts an optional prerelease or build suffix while still rejecting a wrong-architecture asset and the post-quantum signature sidecar."
        },
        {
          "title": "Malformed numeric environment variables are rejected by name",
          "body": "A numeric tunable such as HERMITSTASH_UPLOAD_BYTES_PER_SEC set to a unit-suffixed or non-numeric value (for example `10MB` or `unlimited`) is now rejected at startup with the offending variable named, instead of being silently coerced to a wrong number or dropped while the operator believes it took effect. A blank value still falls back to the documented default, so a container variable left empty continues to start normally."
        },
        {
          "title": "Empty-bundle catch-up completes",
          "body": "A freshly created bundle with no files (a zero-tip catch-up) could leave the engine stuck reporting that it was still catching up. The catch-up-complete transition now fires for an empty bundle."
        },
        {
          "title": "Local rename no longer drops a file when the source delete is gated",
          "body": "When a local rename fell back to delete-plus-recreate and the delete was gated by an in-flight transfer for the same path, the recreated file could be skipped. The fallback now distinguishes a gated delete from a completed one and still uploads the renamed file."
        },
        {
          "title": "Internal scratch files are never uploaded",
          "body": "The case-folding probe and the rename staging file now use the same temporary-file naming the download path uses, so a copy left behind by an interrupted run is swept on the next start and is never mistaken for a real file to upload to the server."
        },
        {
          "title": "Upload and download response bodies use byte-accurate caps",
          "body": "Response bodies are collected against true byte limits instead of a character-length count that could overshoot, a large success response is no longer truncated, and an oversized error body from a failing upload is bounded to the same limit the download path uses."
        }
      ]
    },
    {
      "heading": "Changed",
      "items": [
        {
          "title": "Ignore and include patterns fold case on case-insensitive filesystems",
          "body": "On Windows and macOS, selective-sync ignore and include patterns now match case-insensitively, consistent with how the client already tracks file identity on those filesystems. On case-sensitive filesystems matching stays byte-exact."
        },
        {
          "title": "Client builds on more hardened framework primitives",
          "body": "Path resolution, untrusted-input parsing, bounded response collection, decompression, atomic secret reads, and retry backoff now route through the vendored framework's hardened primitives instead of hand-rolled equivalents, so the client inherits their fail-closed input validation and cross-platform guards directly."
        }
      ]
    },
    {
      "heading": "Supply chain",
      "items": [
        {
          "title": "Vendored framework refreshed to v0.15.15",
          "body": "The single vendored upstream is updated to v0.15.15, which fails more authentication and signature verifiers closed and bounds the WebSocket fragmented-message reassembly the client relies on. The integrity manifest is recomputed against the refreshed bytes."
        },
        {
          "title": "Container image build restored",
          "body": "A committed shell script carried Windows line endings, which failed the shell-lint gate and skipped the container image build for the previous release. Shell scripts are now pinned to line-feed endings repository-wide, and the image-publish lint is scoped to first-party scripts, so the multi-architecture image publishes again."
        }
      ]
    },
    {
      "heading": "Migration",
      "items": [
        {
          "title": "A world-readable credentials fallback file is now refused",
          "body": "If the plaintext credentials fallback file has group- or world-readable permissions, the client now refuses to read it instead of silently tightening it in place. Re-run `hermitstash-sync init`, or restrict the file with `chmod 600`, to restore access. Installs whose credentials file is already owner-only (the normal case) are unaffected."
        }
      ]
    }
  ]
}
