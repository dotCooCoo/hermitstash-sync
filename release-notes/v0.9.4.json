{
  "version": "0.9.4",
  "date": "2026-06-13",
  "headline": "Bounded downloads, local-change ordering, native cert-expiry, and Node 24.16.0",
  "summary": "Server file downloads are now bounded so a runaway body cannot fill the disk, local edits are ordered against same-path server transfers to remove a race, a permanently-rejected WebSocket upgrade surfaces as an error instead of an invisible stuck state, and the runtime floor moves to Node.js 24.16.0 for its OpenSSL and certificate updates.",
  "sections": [
    {
      "heading": "Security",
      "items": [
        {
          "title": "The runtime floor moves to Node.js 24.16.0",
          "body": "This pulls in OpenSSL 3.5.6, updated root certificates (NSS 3.121), additional crypto-validation hardening, and a fix that hardens HTTP client-request option merging against prototype pollution. Pre-built binaries embed it; from-source installs now require Node.js 24.16.0+."
        }
      ]
    },
    {
      "heading": "Fixed",
      "items": [
        {
          "title": "Server downloads are bounded before and during the transfer",
          "body": "A file download is now refused before it starts if its declared size exceeds the per-file limit or the available disk, and aborted mid-stream if the body grows past the limit, instead of writing an unbounded body to disk and only verifying the checksum after the whole thing has landed. The size check no longer depends on the server declaring a size or on the free-space probe succeeding."
        },
        {
          "title": "Local edits are ordered against same-path server transfers",
          "body": "A local file change and a server change to the same path could previously interleave so a local upload raced a concurrent server download. Local changes now run in the same total order as server events and re-check the suppression state after hashing, so the two cannot stomp each other."
        },
        {
          "title": "A rejected WebSocket upgrade surfaces as an error",
          "body": "A permanent WebSocket handshake rejection — a wrong path, a stale bundle, or a server too old to speak the protocol — used to leave the daemon stuck in a reconnecting state forever with no signal. It now transitions to an error state with an actionable message and a metric, while genuinely recoverable cases still reconnect."
        },
        {
          "title": "Resync is safe under overlapping triggers",
          "body": "Two resyncs triggered close together — for example a repeated reload signal — could clear the database twice and reopen each other's protected window. Resync now coalesces overlapping callers onto one operation, and the reload signal handler waits for it to finish."
        },
        {
          "title": "A configuration reload re-scans for changes missed during the watcher restart",
          "body": "Reloading the configuration briefly bounces the file watcher; a local file changed during that window is now re-detected by a rescan instead of being silently missed until the next change."
        },
        {
          "title": "Certificate expiry is detected without an external tool",
          "body": "The client read certificate expiry by spawning the openssl binary, which silently did nothing when openssl was not on the path — common in a single-file binary or a minimal container — so the certificate could expire with no auto-renewal. It now reads the expiry natively, so auto-renewal works everywhere."
        }
      ]
    },
    {
      "heading": "Supply chain",
      "items": [
        {
          "title": "Vendored blamejs advances to v0.15.7",
          "body": "A currency refresh: the framework surfaces this client consumes are unchanged from v0.15.6, so the update carries upstream security fixes (the URL-canonicalizer IPv4-mapped allowlist bypass and server-side correctness gaps) without altering client behavior."
        }
      ]
    }
  ],
  "references": [
    { "label": "blamejs framework", "url": "https://github.com/blamejs/blamejs" }
  ]
}
