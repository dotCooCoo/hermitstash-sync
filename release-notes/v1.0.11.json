{
  "version": "1.0.11",
  "date": "2026-08-10",
  "headline": "Restore the container image build",
  "summary": "The image has not been published since 2026-08-08. Three patterns in the release tooling were written in a shape the unsafe-regex screen refuses, which failed the lint job that gates the image build — while the release workflow carried on publishing binaries, so nothing looked wrong from the outside.",
  "sections": [
    {
      "heading": "Fixed",
      "items": [
        {
          "title": "The container image builds again",
          "body": "Publishing is gated behind a lint job, and that job had been failing since 2026-08-08 on three patterns that put a repetition inside an optional group. The screen refuses that shape on sight, though an optional group repeats at most once and none of the three could actually backtrack badly. They now run through the framework's linear matcher, which cannot backtrack at all, so the work each does is bounded by the length of what it is matched against whatever the pattern looks like — and there is no longer a pattern for the screen to judge. Matching behaviour is unchanged, verified against the platform engine. Releases from v1.0.9 through v1.0.10 published their binaries and signatures normally; only the container image is affected, and pulling either tag now gets an image built from that release."
        },
        {
          "title": "The lint gate can be run before pushing",
          "body": "Its rules were written out by the workflow itself and existed only while that step ran, so there was no way to check a change against them before pushing — which is why the break above went unnoticed for two days. They were also duplicated, once in the test workflow and once in the one that gates the image, free to drift apart from each other as well as from anything local. Both now read a single committed configuration, so `npx eslint .` reproduces exactly what either workflow will do."
        }
      ]
    },
    {
      "heading": "Changed",
      "items": [
        {
          "title": "The vendored framework moves to 0.18.19",
          "body": "Brings the linear matcher used above, and a fix for a request that failed partway through its response being finished off as though it had succeeded."
        },
        {
          "title": "The runtime base image moves to the current Chainguard rebuild",
          "body": "The pinned digest had been superseded upstream. Bumping it picks up whatever package fixes landed in the rebuild."
        }
      ]
    },
    {
      "heading": "Removed",
      "items": [
        {
          "title": "The base-image check no longer advertises --max-age-days",
          "body": "The flag was documented and parsed, but its value was never read, so passing it did nothing. Implementing it needs the build date behind a digest, which that check does not fetch, so the claim is gone rather than left looking like a working control."
        }
      ]
    }
  ]
}
