{
  "version": "0.8.22",
  "date": "2026-05-19",
  "headline": "Resolve SEA SIGTERM exit-code handling under systemd",
  "summary": "Closes out the SIGTERM-exit-code work begun in v0.8.17. The hardened systemd unit now treats Node's SIGTERM-exit-143 from a SEA binary as a clean shutdown via `SuccessExitStatus=143`, so `systemctl stop` no longer flags the daemon as failed.",
  "sections": [
    {
      "heading": "Fixed",
      "items": [
        {
          "title": "`SuccessExitStatus=143` whitelisted in `deploy/hermitstash-sync.service`",
          "body": "Under SEA, Node currently exits with code 143 on a clean SIGTERM shutdown rather than the conventional zero. The shipped systemd unit now treats 143 as success so `systemctl stop hermitstash-sync` reports `inactive (dead)` instead of `failed`, and the external auto-update timer no longer fights a phantom restart loop. The in-binary SEA-SIGTERM detector logs the remap path so operators on bare-binary installs can see why exit 143 appears in their journals."
        }
      ]
    }
  ]
}
