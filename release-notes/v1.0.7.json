{
  "version": "1.0.7",
  "date": "2026-08-08",
  "headline": "Restore the container image, which could not start in 1.0.6",
  "summary": "The 1.0.6 image was missing a shared library that release's binary needs, so the container exited before the entrypoint ran. The image now installs it, and the runtime that builds the binary is pinned so the two cannot drift apart again.",
  "sections": [
    {
      "heading": "Fixed",
      "items": [
        {
          "title": "The 1.0.6 container image could not start",
          "body": "The pre-built binary in 1.0.6 is dynamically linked against libatomic.so.1, which the runtime image did not install. The container failed at load with \"error while loading shared libraries\" before reaching the entrypoint, so it never started at all. The image now installs it. Pull 1.0.7 if you are on the 1.0.6 image; the standalone binaries, the installer, and source installs were not affected."
        }
      ]
    },
    {
      "heading": "Changed",
      "items": [
        {
          "title": "The release build pins the exact runtime that produces the binary",
          "body": "The build resolved its Node version from a range, so it used whichever release was newest on the build machine and crossed a major version between two releases with no accompanying change in this repository. Which shared libraries the pre-built binary needs is decided by that version, which is how the image above came to be missing one. The version is now pinned exactly and stays on the long-term-support line, so a change of runtime has to be a deliberate, visible edit."
        },
        {
          "title": "The installer and updater ask for Node.js 24 or newer",
          "body": "Both use Node only to verify the release signature before installing, and both named a version that has since reached end of life. They now name 24, matching the version this project requires elsewhere. Pre-built binaries embed their own runtime and are unaffected by the version installed on the machine."
        }
      ]
    },
    {
      "heading": "Added",
      "items": [
        {
          "title": "Reconnects no longer wait for the previous connection to time out",
          "body": "The client now sends a stable identifier for this installation when it opens the sync WebSocket. A server that understands it closes this installation's own earlier connection immediately, rather than leaving it to expire while it counts against the per-key connection limit — so reconnecting after a network drop or a restart succeeds straight away instead of being refused until the stale connection is reaped. The identifier is kept alongside the rest of the client's state and survives a restart. It identifies the installation, it does not authenticate it: the API key and client certificate continue to do that. Other installations sharing the same API key are unaffected, and an older server simply ignores it."
        }
      ]
    }
  ]
}
