{
  "version": "0.9.20",
  "date": "2026-06-25",
  "headline": "Restores auto-update on GitHub's current release CDN and hardens remote-delete and download handling against data loss",
  "summary": "A reliability and integrity release for the auto-update and sync data paths. GitHub moved its release-asset content delivery network, and the in-binary self-update only permitted the previous host — so a bare-binary install with auto-update enabled silently stopped installing updates; the egress allowlist now covers GitHub's content CDN by domain suffix and survives a future CDN rename. A remote delete that arrives for a local file the client cannot hash (one that grew past the configured size ceiling, or became a special or unreadable file) is no longer honored by destroying that file; the client keeps it and retries once it can be verified again, and the same protection covers a remote overwrite. A file the server renames into the synced scope without re-sending its checksum is now size-verified before it is committed. No configuration changes are required; bare-binary installs on auto-update resume updating automatically.",
  "sections": [
    {
      "heading": "Fixed",
      "items": [
        {
          "title": "Auto-update works again on GitHub's current release CDN",
          "body": "The in-binary self-update (used by bare-binary installs with auto-update enabled) downloads each release asset from github.com, which redirects to GitHub's signed content CDN. GitHub moved that CDN to a new hostname, and the updater's egress allowlist only permitted the previous one, so the redirect was refused and the update silently never installed — the daemon stayed on its current version while logging only a warning. The allowlist now matches GitHub's content CDN by domain suffix, covering both the previous and current hosts and a future rename, so auto-update resumes. The release signature check (P-384 ECDSA over the SHA3-512 digest, verified against the in-binary public key) remains the integrity gate before any swap. Systemd, Docker, Podman, and Kubernetes installs were unaffected — they update through their own mechanisms."
        },
        {
          "title": "A remote delete no longer destroys a local file it cannot verify",
          "body": "Before honoring a server-side delete, the client hashes the local file to check whether you changed it independently (in which case it keeps your copy as a conflict file). If that hash could not be computed — the file had grown past the configured size ceiling, or had become a special or unreadable file — the client could not confirm the file was unmodified, yet it deleted the file anyway, losing locally-changed bytes with no conflict copy. It now keeps the file and its tracking row and re-tries the delete on the next reconnect once the file is verifiable again. The conflict-copy path that protects a local file from a remote overwrite is hardened the same way."
        }
      ]
    },
    {
      "heading": "Security",
      "items": [
        {
          "title": "A file received without a checksum is size-verified before being committed",
          "body": "Every downloaded file is verified against its SHA3-512 checksum before being written into place. On one selective-sync edge — when the server renames a file from a path you do not sync into one you do, and omits the checksum because the content is unchanged — the client had no checksum to verify against and committed the bytes unconditionally, so a truncated or corrupt response body could land marked as synced. The exact byte count is now compared to the size the server declared, and a mismatch is refused. The transport is already PQC TLS 1.3 with mutual authentication, so this closes the residual gap of a half-failed response body rather than an active attacker."
        }
      ]
    }
  ]
}
