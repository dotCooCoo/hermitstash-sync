{
  "version": "0.8.5",
  "date": "2026-05-18",
  "headline": "Fix bandwidth limiter double-spend under concurrent uploads",
  "sections": [
    {
      "heading": "Fixed",
      "items": [
        {
          "title": "Token-bucket increment under parallel uploads",
          "body": "The bandwidth limiter introduced in v0.8.3 used a non-atomic check-then-decrement against the token bucket; under the parallel-upload concurrency added in v0.8.2 two uploads could spend the same token, causing the limiter to overshoot the configured cap by up to `concurrency - 1` tokens per refill window. The bucket update is now serialized through the limiter's own queue so each token is only spent once."
        }
      ]
    }
  ]
}
