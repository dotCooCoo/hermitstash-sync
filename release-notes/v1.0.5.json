{
  "version": "1.0.5",
  "date": "2026-08-06",
  "headline": "Reach the server inside a Tailscale or WireGuard tunnel",
  "summary": "Sync clients that reach the HermitStash server over a Tailscale/WireGuard tunnel can now verify it by a pinned server key instead of the tunnel hostname, which the server's external TLS certificate does not carry.",
  "sections": [
    {
      "heading": "Added",
      "items": [
        {
          "title": "Tunnel mode",
          "body": "When the server is reached inside a Tailscale (or other WireGuard) tunnel — on its MagicDNS name or tailnet IP — its external TLS leaf will not carry that hostname, so the normal hostname check fails. Tunnel mode skips only that hostname match while still enforcing the pinned server key, the mTLS chain, and the post-quantum group floor; the tunnel, the pin, and mTLS bind the server's identity. Enable it at enrollment with \"hermitstash-sync init --tunnel\", or HERMITSTASH_TUNNEL_MODE=1 for a headless setup. It is explicit opt-in only — never inferred from a URL — and fail-closed: turning it on without a pinned server key is refused, because relaxing the hostname check with no pin would accept any certificate that chains to a trusted authority."
        },
        {
          "title": "Server key pinning from an out-of-band value, or from a confirmed one",
          "body": "Tunnel mode prefers a pin supplied through HERMITSTASH_PINNED_SERVER_SPKI, read off the server beforehand; when one is supplied no probe is made. With no pin supplied, interactive setup shows the key it observes and asks you to confirm it matches the server. A headless setup requires the supplied pin — it has nobody to compare an observed key against, and a proceed-anyway flag would only mean pin whatever answers, which names no value and so cannot tell your server from an interceptor. Because tunnel mode does not check the hostname, that pin is what binds the server's identity: anyone able to intercept the connection during setup could otherwise have their own key pinned, and the interception would persist on every later connection instead of failing visibly."
        }
      ]
    },
    {
      "heading": "Security",
      "items": [
        {
          "title": "TLS options in the config file no longer override server verification",
          "body": "The config file could carry TLS options for the WebSocket transport, and they were applied last — so they overrode the client's own settings. Setting rejectUnauthorized to false disabled certificate-chain validation on that connection and skipped the server-key pin check along with it; a ca entry would instead replace the trust anchors with an attacker's root, servername would choose the name the identity check validates against, and a version or cipher setting would drop the connection to TLS 1.2. Each yields an intercepted control channel, and with it the API key, which on macOS and Windows is otherwise held in the operating system keychain. TLS options from the config file are now ignored outright and logged rather than applied. Set HERMITSTASH_ALLOW_INSECURE_TLS=1 to apply them deliberately, for a self-signed test server."
        }
      ]
    },
    {
      "heading": "Changed",
      "items": [
        {
          "title": "Refreshed the vendored blamejs framework to 0.18.14",
          "body": "Keeps the vendored framework current, including its refreshed elliptic-curve and certificate bundles. The primitives the client relies on — the WebSocket transport, JSON parsing, URL and path safety, and the certificate and crypto bootstrap — are unchanged, so the sync behavior is identical."
        }
      ]
    }
  ]
}
