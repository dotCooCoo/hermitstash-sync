"use strict";

module.exports = {
  slug:  "guard-cidr",
  title: "CIDR identifier safety",
  body: [
  '<h1>CIDR identifier safety</h1>',
  '<p><code>b.guardCidr</code> validates user-supplied CIDR notation strings (IPv4 + IPv6) destined for network allowlists, ACLs, security-group rules, and tenant-boundary configuration. KIND="identifier" — consumes <code>ctx.identifier</code> (or <code>ctx.cidr</code>). Threat catalog: shape malformation, octet/group overflow, mask out-of-range, network-address misalignment (host bits set), reserved-range membership (RFC 1918 / loopback / link-local / multicast / documentation / benchmarking / CGNAT / IPv6 ULA / fe80 link-local / multicast / documentation), IPv4-mapped IPv6 dual-stack confusion (CVE-2021-22931 IPv6 variant), BIDI / zero-width / control / null-byte universal refuse. Auto-registers into <code>b.guardAll</code> as a STANDALONE_GUARD.</p>',

  '<h2 id="validate">b.guardCidr.validate(cidr, opts) <a class="anchor" href="#validate">#</a></h2>',
  '<pre><code class="language-javascript">{',
  '  profile:                "strict" | "balanced" | "permissive",',
  '  compliancePosture:      "hipaa" | "pci-dss" | "gdpr" | "soc2",',
  '  family:                 "either" | "ipv4-only" | "ipv6-only",',
  '  networkAlignmentPolicy: "reject" | "audit" | "allow",',
  '  reservedRangesPolicy:   "reject" | "audit" | "allow",',
  '  ipv4MappedIpv6Policy:   "reject" | "audit" | "allow",',
  '  requireMaskPolicy:      "reject-bare-ip" | "audit-bare-ip" | "allow-bare-ip",',
  '  bidiPolicy:             "reject" | "allow",          // refused at every profile',
  '  controlPolicy:          "reject" | "allow",          // refused at every profile',
  '  nullBytePolicy:         "reject" | "allow",          // refused at every profile',
  '  zeroWidthPolicy:        "reject" | "allow",          // refused at every profile',
  '  maxBytes:               number,                       // total input cap (default 64)',
  '}</code></pre>',
  '<p>Validates a CIDR string. Returns <code>{ ok, issues }</code>. Issue kinds: <code>bad-input</code>, <code>empty</code>, <code>cidr-cap</code>, <code>address-shape</code>, <code>mask-shape</code>, <code>mask-cap</code>, <code>bare-ip</code>, <code>family-mismatch</code>, <code>network-misaligned</code>, <code>reserved-range</code>, <code>ipv4-mapped-ipv6</code>, <code>bidi-override</code>, <code>null-byte</code>, <code>control-char</code>, <code>zero-width</code>.</p>',
  '<pre><code class="language-javascript">b.guardCidr.validate("8.8.8.0/24", { profile: "strict" });',
  '// → { ok: true, issues: [] }',
  '',
  '// RFC 1918 + loopback + link-local + multicast — all refused at strict.',
  'b.guardCidr.validate("10.0.0.0/8",     { profile: "strict" }); // reserved-range',
  'b.guardCidr.validate("127.0.0.0/8",    { profile: "strict" }); // reserved-range',
  'b.guardCidr.validate("169.254.0.0/16", { profile: "strict" }); // reserved-range',
  'b.guardCidr.validate("224.0.0.0/4",    { profile: "strict" }); // reserved-range',
  '',
  '// Misaligned network address — common typo class.',
  'b.guardCidr.validate("8.8.8.1/24", { profile: "strict" });',
  '// → ok=false; network-misaligned (24-bit prefix has host bits set)',
  '',
  '// IPv6 reserved + IPv4-mapped IPv6 confusion.',
  'b.guardCidr.validate("2001:db8::/32",          { profile: "strict" }); // documentation',
  'b.guardCidr.validate("fc00::/7",               { profile: "strict" }); // ULA',
  'b.guardCidr.validate("::ffff:192.0.2.1/128",   { profile: "strict" }); // ipv4-mapped-ipv6',
  '</code></pre>',

  '<h2 id="sanitize">b.guardCidr.sanitize(cidr, opts) <a class="anchor" href="#sanitize">#</a></h2>',
  '<pre><code class="language-javascript">{',
  '  profile:           "strict" | "balanced" | "permissive",',
  '  compliancePosture: "hipaa" | "pci-dss" | "gdpr" | "soc2",',
  '}</code></pre>',
  '<p>Throws on any high / critical issue. On safe input, normalizes IPv6 to lowercase form. Returns the normalized string.</p>',
  '<pre><code class="language-javascript">b.guardCidr.sanitize("2606:4700:4700::1111/128", { profile: "balanced" });',
  '// → "2606:4700:4700::1111/128"',
  '</code></pre>',

  '<h2 id="gate">b.guardCidr.gate(opts) <a class="anchor" href="#gate">#</a></h2>',
  '<pre><code class="language-javascript">{',
  '  profile:               "strict" | "balanced" | "permissive",',
  '  compliancePosture:     "hipaa" | "pci-dss" | "gdpr" | "soc2",',
  '  mode:                  "enforce" | "warn-only" | "shadow" | "audit-only" | "log-only" | "canary",',
  '  audit:                 b.audit,',
  '  observability:         b.observability,',
  '  forensicEvidenceStore: { put }, forensicSnippetBytes: number,',
  '  cache:                 b.cache, cacheTtlMs: number,',
  '  maxRuntimeMs:          number,',
  '}</code></pre>',
  '<p>Returns a <code>b.gateContract</code>-shaped gate. Reads <code>ctx.identifier</code> (or <code>ctx.cidr</code>); empty / missing identifier resolves to <code>{ ok: true, action: "serve" }</code>.</p>',
  '<pre><code class="language-javascript">var cidrGate = b.guardCidr.gate({ profile: "strict", audit: b.audit });',
  '',
  'app.post("/admin/firewall/allowlist", async function (req, res) {',
  '  var rv = await cidrGate.check({ identifier: req.body.cidr });',
  '  if (!rv.ok) return res.status(400).json({ refused: rv.issues });',
  '  // ... persist allowlisted CIDR ...',
  '});</code></pre>',

  '<h2 id="threat-catalog">Threat catalog <a class="anchor" href="#threat-catalog">#</a></h2>',
  '<ul>',
  '<li><strong>Shape malformation</strong> — input is not <code>address/mask</code>; address is not valid IPv4 dotted-decimal or IPv6 hex-group form.</li>',
  '<li><strong>IPv4 octet overflow</strong> — octet > 255 / leading-zero forms (octal/hex/long forms refused; reach for <code>b.guardDomain</code> if those are needed elsewhere).</li>',
  '<li><strong>Mask out of range</strong> — IPv4 prefix > 32; IPv6 prefix > 128; non-numeric mask.</li>',
  '<li><strong>Network-address misalignment</strong> — <code>10.0.0.1/24</code> carries host bits set when /24 implies the pure network address <code>10.0.0.0/24</code>. Common typo class with surprising allowlist semantics.</li>',
  '<li><strong>RFC 1918 private ranges</strong> — <code>10.0.0.0/8</code>, <code>172.16.0.0/12</code>, <code>192.168.0.0/16</code> — internal-network leak class.</li>',
  '<li><strong>IPv4 reserved ranges</strong> — loopback <code>127.0.0.0/8</code>, link-local <code>169.254.0.0/16</code>, multicast <code>224.0.0.0/4</code>, class-E <code>240.0.0.0/4</code>, documentation TEST-NET 1/2/3 (RFC 5737), benchmarking <code>198.18.0.0/15</code>, CGNAT <code>100.64.0.0/10</code>, this-network <code>0.0.0.0/8</code>.</li>',
  '<li><strong>IPv6 reserved ranges</strong> — loopback <code>::1</code>, unspecified <code>::/128</code>, ULA <code>fc00::/7</code>, link-local <code>fe80::/10</code>, multicast <code>ff00::/8</code>, IPv4-mapped <code>::ffff:0:0/96</code>, documentation <code>2001:db8::/32</code>, teredo, deprecated 6to4 <code>2002::/16</code>.</li>',
  '<li><strong>IPv4-mapped IPv6 confusion</strong> — <code>::ffff:192.168.1.1</code> represents an IPv4 address in IPv6 namespace and trips dual-stack allowlist matchers (CVE-2021-22931 IPv6 variant).</li>',
  '<li><strong>Bare IP without mask</strong> — strict refuses (use /32 or /128 for a single host); balanced audits; permissive allows.</li>',
  '<li><strong>BIDI / zero-width / control / null-byte</strong> — universal-refuse same codepoint catalog as the rest of the family.</li>',
  '</ul>',

  '<h2 id="profiles">Profiles <a class="anchor" href="#profiles">#</a></h2>',
  '<p><strong>strict</strong> — refuse misalignment, refuse all reserved ranges, refuse IPv4-mapped IPv6, refuse bare IP without mask.</p>',
  '<p><strong>balanced</strong> — audit misalignment + reserved + IPv4-mapped + bare IP; everything else accepted.</p>',
  '<p><strong>permissive</strong> — universal-refuse class (BIDI / control / null / zero-width) still refused; reserved / misaligned / IPv4-mapped / bare-IP all allowed.</p>',

  '<h2 id="compliance">Compliance postures <a class="anchor" href="#compliance">#</a></h2>',
  '<p><strong>hipaa</strong> / <strong>pci-dss</strong> / <strong>soc2</strong> — strict overlay + forensic snapshots (128 / 128 / 256 bytes).</p>',
  '<p><strong>gdpr</strong> — balanced overlay + 64-byte forensic snippets.</p>',

  ].join("\n"),
};
