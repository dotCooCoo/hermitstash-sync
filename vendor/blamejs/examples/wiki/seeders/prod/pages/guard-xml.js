"use strict";

module.exports = {
  slug:  "guard-xml",
  title: "XML content safety",
  body: [
  '<h1>XML content safety</h1>',
  '<p><code>b.guardXml</code> is the XML content-safety primitive. Threat catalog grounded in current research (CVE-2026-24400 AssertJ XXE; CVE-2025-3225 sitemap parser; CVE-2024-1455 LangChain; CVE-2024-25062 libxml2 use-after-free with DTD + XInclude; CVE-2024-56171 + CVE-2025-24928 + CVE-2025-32415 + CVE-2025-27113 libxml2 family; CVE-2024-8176 libexpat stack overflow via recursive entity expansion). Auto-registers into <code>b.guardAll</code>.</p>',

  '<h2 id="validate">b.guardXml.validate(input, opts) <a class="anchor" href="#validate">#</a></h2>',
  '<pre><code class="language-javascript">{',
  '  profile:                "strict" | "balanced" | "permissive",',
  '  compliancePosture:      "hipaa" | "pci-dss" | "gdpr" | "soc2",',
  '  doctypePolicy:          "reject" | "allow",          // refused at every profile',
  '  entityPolicy:           "reject" | "allow",',
  '  externalEntityPolicy:   "reject" | "allow",',
  '  xincludePolicy:         "reject" | "audit" | "allow",',
  '  schemaLocationPolicy:   "reject" | "audit" | "allow",',
  '  processingInstrPolicy:  "reject" | "audit" | "allow",',
  '  cdataPolicy:            "reject" | "audit" | "allow",',
  '  xmlDsigPolicy:          "audit" | "allow",',
  '  bidiPolicy:             "reject" | "strip" | "audit" | "allow",',
  '  controlPolicy:          "reject" | "strip" | "audit" | "allow",',
  '  nullBytePolicy:         "reject" | "strip" | "audit" | "allow",',
  '  zeroWidthPolicy:        "reject" | "strip" | "audit" | "allow",',
  '  maxBytes:               number,',
  '  maxDepth:               number,',
  '  maxElements:            number,',
  '  maxAttrsPerElement:     number,',
  '  maxAttrValueBytes:      number,',
  '}</code></pre>',
  '<p>Returns <code>{ ok, issues }</code>. Issue kinds: <code>doctype</code>, <code>entity-declaration</code>, <code>parameter-entity</code>, <code>external-entity</code>, <code>xinclude</code>, <code>schema-location</code>, <code>processing-instruction</code>, <code>cdata</code>, <code>xml-signature</code>, <code>bidi-override</code>, <code>null-byte</code>, <code>control-char</code>, <code>element-cap</code>, <code>depth-cap</code>, <code>too-large</code>.</p>',
  '<pre><code class="language-javascript">b.guardXml.validate(\'<?xml version="1.0"?><root><x>1</x></root>\', { profile: "strict" });',
  '// → { ok: true, issues: [] }',
  '',
  '// Hostile: DOCTYPE with internal-subset entity (XXE + billion-laughs vector).',
  'b.guardXml.validate(\'<?xml version="1.0"?><!DOCTYPE r [<!ENTITY x "y">]><r/>\', { profile: "strict" });',
  '// → ok=false; issues include doctype + entity-declaration',
  '</code></pre>',

  '<h2 id="sanitize">b.guardXml.sanitize(input, opts) <a class="anchor" href="#sanitize">#</a></h2>',
  '<pre><code class="language-javascript">{',
  '  profile:                "strict" | "balanced" | "permissive",',
  '  bidiPolicy:             "reject" | "strip" | "audit" | "allow",',
  '  controlPolicy:          "reject" | "strip" | "audit" | "allow",',
  '  nullBytePolicy:         "reject" | "strip" | "audit" | "allow",',
  '  zeroWidthPolicy:        "reject" | "strip" | "audit" | "allow",',
  '}</code></pre>',
  '<p>Strips character-class threats per policy but ALWAYS THROWS on critical (DOCTYPE / ENTITY / external-entity / parameter-entity) — these have no safe sanitization, the only correct response is reject. Returns the cleaned XML string when no critical threats are present.</p>',
  '<pre><code class="language-javascript">// Throws — no safe sanitization for DOCTYPE.',
  'try { b.guardXml.sanitize(\'<?xml version="1.0"?><!DOCTYPE r><r/>\', { profile: "balanced" }); }',
  'catch (e) { e.code; }   // "xml.doctype"',
  '</code></pre>',

  '<h2 id="gate">b.guardXml.gate(opts) <a class="anchor" href="#gate">#</a></h2>',
  '<pre><code class="language-javascript">{',
  '  profile:               "strict" | "balanced" | "permissive",',
  '  compliancePosture:     "hipaa" | "pci-dss" | "gdpr" | "soc2",',
  '  mode:                  "enforce" | "warn-only" | "shadow" | "audit-only" | "log-only" | "canary",',
  '  audit:                 b.audit,',
  '  observability:         b.observability,',
  '  forensicEvidenceStore: { put }, forensicSnippetBytes: number,',
  '  cache:                 b.cache, cacheTtlMs: number,',
  '  maxRuntimeMs:          number,',
  '  beforeCheck/afterCheck/onIssue/onSanitize/onRefuse/onAudit: hooks,',
  '}</code></pre>',
  '<p>Returns a <code>b.gateContract</code>-shaped gate. Auto-routed via <code>b.guardAll</code> when Content-Type is <code>application/xml</code> / <code>text/xml</code>. As of v0.7.12 wired by default into <code>b.fileUpload</code> + <code>b.staticServe</code> at strict profile.</p>',
  '<pre><code class="language-javascript">var xmlGate = b.guardXml.gate({ profile: "strict", audit: b.audit });</code></pre>',

  '<h2 id="threat-catalog">Threat catalog <a class="anchor" href="#threat-catalog">#</a></h2>',
  '<ul>',
  '<li><strong>DOCTYPE declarations</strong> — refused unconditionally regardless of profile (XXE / billion-laughs vector). CVE-2026-24400 AssertJ class.</li>',
  '<li><strong>&lt;!ENTITY&gt; declarations</strong> including parameter entities (% prefix — out-of-band exfil vector).</li>',
  '<li><strong>External entity references</strong> — SYSTEM / PUBLIC identifiers with file:// / http:// / ftp:// / gopher:// schemes (XXE — local file read + SSRF).</li>',
  '<li><strong>XInclude</strong> — <code>&lt;xi:include href="..."/&gt;</code> remote inclusion. CVE-2024-25062 libxml2 use-after-free class.</li>',
  '<li><strong>xsi:schemaLocation</strong> / xsi:noNamespaceSchemaLocation — operator-controlled schema fetch.</li>',
  '<li><strong>Processing instructions</strong> — <code>&lt;?xml-stylesheet ...?&gt;</code> CSS injection vector. Standard <code>&lt;?xml?&gt;</code> declaration not flagged.</li>',
  '<li><strong>CDATA sections</strong> — often used to hide payloads from naive scanners.</li>',
  '<li><strong>XML signature wrapping</strong> (xmldsig) — surface that requires careful operator handling; flagged as audit.</li>',
  '<li><strong>Bidi / null / control / zero-width</strong> chars in element text + attribute values — same codepoint catalog as guard-csv/html/svg/json/yaml.</li>',
  '<li><strong>Anti-DoS caps</strong> — total document size, max element count, max nesting depth, per-attribute-value length.</li>',
  '</ul>',

  '<h2 id="profiles">Profiles <a class="anchor" href="#profiles">#</a></h2>',
  '<p><strong>strict</strong> — refuse every threat (DOCTYPE / ENTITY / external / XInclude / schemaLocation / PIs / CDATA / bidi / null / control); 2 MiB cap; depth 64; 8192 elements; 64 attrs/element.</p>',
  '<p><strong>balanced</strong> — DOCTYPE/ENTITY/external still refused (universal billion-laughs class); audit XInclude/schemaLocation/PIs/CDATA; strip char-class threats; 8 MiB cap; depth 256.</p>',
  '<p><strong>permissive</strong> — DOCTYPE/ENTITY/external still refused; audit most others; refuse only null bytes; 64 MiB cap; depth 1024.</p>',

  '<h2 id="compliance">Compliance postures <a class="anchor" href="#compliance">#</a></h2>',
  '<p><strong>hipaa</strong> / <strong>pci-dss</strong> / <strong>soc2</strong> — strict overlay + forensic snapshots (256 / 256 / 512 bytes).</p>',
  '<p><strong>gdpr</strong> — balanced overlay + 128-byte forensic snippets.</p>',

  ].join("\n"),
};
