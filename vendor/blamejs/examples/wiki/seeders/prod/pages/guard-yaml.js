"use strict";

module.exports = {
  slug:  "guard-yaml",
  title: "YAML content safety",
  body: [
  '<h1>YAML content safety</h1>',
  '<p><code>b.guardYaml</code> is the YAML content-safety primitive. Threat catalog grounded in current research (CVE-2026-24009 Docling/PyYAML unsafe load → RCE; CVE-2026-27807 MarkUs alias billion-laughs DoS; CVE-2025-68664 LangChain deserialization → RCE; CVE-2025-61301 + CVE-2025-61303 YAML library DoS family — "Laughter in the Wild" study; CVE-2022-1471 SnakeYAML constructor RCE; CVE-2020-1747 + CVE-2020-14343 PyYAML FullLoader; CVE-2017-18342 PyYAML python/object/apply). Auto-registers into <code>b.guardAll</code>.</p>',

  '<h2 id="validate">b.guardYaml.validate(input, opts) <a class="anchor" href="#validate">#</a></h2>',
  '<pre><code class="language-javascript">{',
  '  profile:                "strict" | "balanced" | "permissive",',
  '  compliancePosture:      "hipaa" | "pci-dss" | "gdpr" | "soc2",',
  '  tagPolicy:              "reject" | "audit" | "allow",',
  '  aliasPolicy:            "reject" | "audit" | "allow",',
  '  multiDocPolicy:         "reject" | "audit" | "allow",',
  '  norwayPolicy:           "reject" | "audit" | "allow",',
  '  leadingZeroPolicy:      "reject" | "audit" | "allow",',
  '  duplicateKeyPolicy:     "reject" | "audit" | "allow",',
  '  mergeKeyPolicy:         "reject" | "audit" | "allow",',
  '  bidiPolicy:             "reject" | "strip" | "audit" | "allow",',
  '  controlPolicy:          "reject" | "strip" | "audit" | "allow",',
  '  nullBytePolicy:         "reject" | "strip" | "audit" | "allow",',
  '  zeroWidthPolicy:        "reject" | "strip" | "audit" | "allow",',
  '  safeCoreTagsAllowed:    boolean,',
  '  maxBytes:               number,',
  '  maxDepth:               number,',
  '  maxAnchors:             number,',
  '  maxAliasDepth:          number,',
  '  maxDocuments:           number,',
  '  maxNodes:               number,',
  '  maxScalarLength:        number,',
  '}</code></pre>',
  '<p>Returns <code>{ ok, issues }</code>. Issue kinds: <code>dangerous-tag</code>, <code>custom-tag</code>, <code>core-tag</code>, <code>anchor-cap</code>, <code>alias-disabled</code>, <code>alias-explosion</code>, <code>multi-document</code>, <code>norway-implicit-bool</code>, <code>leading-zero-octal</code>, <code>merge-key</code>, <code>duplicate-key</code>, <code>bidi-override</code>, <code>null-byte</code>, <code>control-char</code>, <code>parse-failed</code>, <code>too-large</code>.</p>',
  '<pre><code class="language-javascript">b.guardYaml.validate("name: alice\\nage: 30\\n", { profile: "strict" });',
  '// → { ok: true, issues: [] }',
  '',
  '// Hostile: deserialization-tag injection.',
  'b.guardYaml.validate("!!python/object/new:cls\\nargs: [a]\\n", { profile: "strict" });',
  '// → ok=false; issues include dangerous-tag (CVE-2026-24009 / CVE-2022-1471 class)',
  '',
  '// Norway problem.',
  'b.guardYaml.validate("country: NO\\n", { profile: "strict" });',
  '// → ok=false; issues include norway-implicit-bool',
  '</code></pre>',

  '<h2 id="parse">b.guardYaml.parse(input, opts) <a class="anchor" href="#parse">#</a></h2>',
  '<pre><code class="language-javascript">{',
  '  profile:    "strict" | "balanced" | "permissive",',
  '  tagPolicy:  "reject" | "audit" | "allow",',
  '  aliasPolicy: "reject" | "audit" | "allow",',
  '  norwayPolicy: "reject" | "audit" | "allow",',
  '  duplicateKeyPolicy: "reject" | "audit" | "allow",',
  '  bidiPolicy: "reject" | "strip" | "audit" | "allow",',
  '  maxBytes:   number,',
  '  maxDepth:   number,',
  '  maxNodes:   number,',
  '}</code></pre>',
  '<p>Refuses on critical issues per policy, then parses through the framework\'s safe-subset YAML parser (no tags, no aliases by design). Throws <code>GuardYamlError</code>.</p>',
  '<pre><code class="language-javascript">b.guardYaml.parse("name: alice\\nage: 30\\n", { profile: "strict" });',
  '// → { name: "alice", age: 30 }',
  '',
  'try { b.guardYaml.parse("!!python/object/new:cls\\nargs: [a]\\n", { profile: "strict" }); }',
  'catch (e) { e.code; }   // "yaml.dangerous-tag"',
  '</code></pre>',

  '<h2 id="gate">b.guardYaml.gate(opts) <a class="anchor" href="#gate">#</a></h2>',
  '<pre><code class="language-javascript">{',
  '  profile:               "strict" | "balanced" | "permissive",',
  '  compliancePosture:     "hipaa" | "pci-dss" | "gdpr" | "soc2",',
  '  mode:                  "enforce" | "warn-only" | "shadow" | "audit-only" | "log-only" | "canary",',
  '  audit:                 b.audit,',
  '  observability:         b.observability,',
  '  forensicEvidenceStore: { put }, forensicSnippetBytes: number,',
  '  cache:                 b.cache, cacheTtlMs: number,',
  '  maxRuntimeMs:          number,',
  '  beforeCheck/afterCheck/onIssue/onSanitize/onRefuse/onAudit: hooks,',
  '}</code></pre>',
  '<p>Returns a <code>b.gateContract</code>-shaped gate. YAML has no safe sanitization — gate decisions are <code>serve</code> / <code>audit-only</code> / <code>refuse</code> only (no <code>sanitize</code> action). As of v0.7.12, every <code>b.fileUpload</code> + <code>b.staticServe</code> deploy gets this gate at strict profile by default — no explicit wiring required.</p>',
  '<pre><code class="language-javascript">var yamlGate = b.guardYaml.gate({ profile: "strict", audit: b.audit });</code></pre>',

  '<h2 id="threat-catalog">Threat catalog <a class="anchor" href="#threat-catalog">#</a></h2>',
  '<ul>',
  '<li><strong>Tag-injection RCE</strong> — language-specific deserialization tags with prefixes <code>!!python/</code> / <code>!!java.</code> / <code>!!ruby/</code> / <code>!!perl/</code> / <code>!!js/</code> / <code>!!cs/</code> / <code>!!net/</code> / <code>!!system.</code> and the <code>!!apply</code> / <code>!!new</code> family. Refused regardless of profile under strict.</li>',
  '<li><strong>Anchor / alias recursion</strong> — billion-laughs amplification via <code>&amp;anchor</code> + <code>*alias</code> chains. Caps via maxAnchors + maxAliasDepth + alias-amplification ratio (≥8x flagged as alias-explosion).</li>',
  '<li><strong>Multi-document streams</strong> — operators expecting one doc silently get the first one and ignore the rest, masking hostile content.</li>',
  '<li><strong>Norway problem</strong> — YAML 1.1 (still default in pyyaml + libyaml in 2026) treats unquoted no/yes/y/n/on/off as booleans. Country code "NO" → false.</li>',
  '<li><strong>Leading-zero octals</strong> — 0777 parses as octal 511 in YAML 1.1.</li>',
  '<li><strong>Duplicate keys</strong> — YAML 1.2 SHOULD-unique; parsers silently last-wins (same threat shape as JSON duplicate-key smuggling).</li>',
  '<li><strong>Custom + local tags</strong> — even when not language-specific, surface that suggests a non-safe parser is downstream.</li>',
  '<li><strong>Merge-key chain depth</strong> (<code>&lt;&lt;: *anchor</code>) — anchor-chain DoS.</li>',
  '<li><strong>Bidi / null / control / zero-width</strong> in scalar values — same codepoint catalog as guard-csv/html/svg/json.</li>',
  '<li><strong>Anti-DoS caps</strong> — total document size, total node count, max anchors, max alias depth, max document count, max scalar length, max depth.</li>',
  '</ul>',

  '<h2 id="profiles">Profiles <a class="anchor" href="#profiles">#</a></h2>',
  '<p><strong>strict</strong> — refuse every threat (tags / aliases / multi-doc / Norway / leading-zero / duplicates / merge keys / bidi / null / control); 2 MiB doc cap; depth 8; 16 anchors; 1 alias depth; 1 document; 1024 nodes.</p>',
  '<p><strong>balanced</strong> — audit most threats; allow YAML 1.2 core tags (<code>!!str</code> / <code>!!int</code> / <code>!!float</code> / <code>!!bool</code> / <code>!!null</code> / <code>!!seq</code> / <code>!!map</code> / etc.); strip bidi/control/null/zero-width; refuse alias explosion (≥8x ratio); 8 MiB cap; depth 32; 64 anchors; 16 documents.</p>',
  '<p><strong>permissive</strong> — audit most; refuse only null bytes and alias explosion outright; 64 MiB cap; depth 64; 1024 anchors.</p>',

  '<h2 id="compliance">Compliance postures <a class="anchor" href="#compliance">#</a></h2>',
  '<p><strong>hipaa</strong> / <strong>pci-dss</strong> / <strong>soc2</strong> — strict overlay + forensic snapshots (256 / 256 / 512 bytes).</p>',
  '<p><strong>gdpr</strong> — balanced overlay + 128-byte forensic snippets.</p>',

  ].join("\n"),
};
