"use strict";

module.exports = {
  slug:  "guard-shell",
  title: "Shell argument safety",
  body: [
  '<h1>Shell argument safety</h1>',
  '<p><code>b.guardShell</code> validates user-input strings BEFORE they\'re handed to a child-process spawn (regardless of operator\'s <code>shell:</code> opt). The canonical defense is "use array args + <code>shell:false</code>", but operators still receive operator-untrusted strings flowing through path-arg or arg-list shapes — guardShell refuses obvious shell-injection shapes before the spawn call. KIND="identifier" — consumes <code>ctx.identifier</code> (or <code>ctx.arg</code>). Threat catalog: POSIX + cmd.exe metacharacters, $(...) / ${...} command + parameter substitution, backtick substitution, process substitution, $VAR parameter expansion, newline injection, leading-hyphen option-flag injection, BIDI / null / control / zero-width universal refuse.</p>',

  '<h2 id="validate">b.guardShell.validate(arg, opts) <a class="anchor" href="#validate">#</a></h2>',
  '<pre><code class="language-javascript">{',
  '  profile:             "strict" | "balanced" | "permissive",',
  '  compliancePosture:   "hipaa" | "pci-dss" | "gdpr" | "soc2",',
  '  posixMetaPolicy:     "reject" | "audit" | "allow",',
  '  cmdMetaPolicy:       "reject" | "audit" | "allow",',
  '  dollarSubstPolicy:   "reject" | "allow",          // refused at every profile',
  '  processSubstPolicy:  "reject" | "allow",          // refused at every profile',
  '  backtickPolicy:      "reject" | "allow",          // refused at every profile',
  '  newlinePolicy:       "reject" | "allow",          // refused at every profile',
  '  argHyphenPolicy:     "reject" | "audit" | "allow",',
  '  bidiPolicy:          "reject" | "allow",          // refused at every profile',
  '  controlPolicy:       "reject" | "allow",          // refused at every profile',
  '  nullBytePolicy:      "reject" | "allow",          // refused at every profile',
  '  zeroWidthPolicy:     "reject" | "allow",          // refused at every profile',
  '  maxBytes:            number,                       // default 2 KiB (8 KiB permissive)',
  '}</code></pre>',
  '<p>Validates a single shell-argument string. Returns <code>{ ok, issues }</code>. Issue kinds: <code>bad-input</code>, <code>shell-cap</code>, <code>posix-metachar</code>, <code>cmd-metachar</code>, <code>dollar-substitution</code>, <code>backtick</code>, <code>process-substitution</code>, <code>dollar-var</code>, <code>newline</code>, <code>arg-hyphen-leading</code>, plus the universal codepoint-class kinds.</p>',
  '<pre><code class="language-javascript">b.guardShell.validate("safe-arg-value", { profile: "strict" });',
  '// → { ok: true, issues: [] }',
  '',
  '// Command-injection chain.',
  'b.guardShell.validate("safe; rm -rf /", { profile: "strict" });',
  '// → ok=false; posix-metachar + cmd-metachar',
  '',
  '// Command + parameter substitution — universal refuse.',
  'b.guardShell.validate("input$" + "(whoami)",  { profile: "strict" });',
  'b.guardShell.validate("input$" + "{HOME}",    { profile: "strict" });',
  'b.guardShell.validate("input`whoami`",        { profile: "strict" });',
  'b.guardShell.validate("file<(cat /etc/pw)",   { profile: "strict" });',
  '// → ok=false; (dollar-substitution | backtick | process-substitution)',
  '',
  '// Leading-hyphen option-flag injection.',
  'b.guardShell.validate("-rf",         { profile: "strict" });',
  'b.guardShell.validate("--exec=cmd",  { profile: "strict" });',
  '// → ok=false; arg-hyphen-leading',
  '</code></pre>',

  '<h2 id="sanitize">b.guardShell.sanitize(arg, opts) <a class="anchor" href="#sanitize">#</a></h2>',
  '<pre><code class="language-javascript">{',
  '  profile:           "strict" | "balanced" | "permissive",',
  '  compliancePosture: "hipaa" | "pci-dss" | "gdpr" | "soc2",',
  '}</code></pre>',
  '<p>Shell args can\'t be repaired — <code>sanitize</code> either passes through valid input or throws.</p>',
  '<pre><code class="language-javascript">var arg = "user-supplied-filename.txt";',
  'var safe = b.guardShell.sanitize(arg, { profile: "strict" });',
  '// safe === arg when validation passes; throws otherwise.</code></pre>',

  '<h2 id="gate">b.guardShell.gate(opts) <a class="anchor" href="#gate">#</a></h2>',
  '<pre><code class="language-javascript">{',
  '  profile:               "strict" | "balanced" | "permissive",',
  '  compliancePosture:     "hipaa" | "pci-dss" | "gdpr" | "soc2",',
  '  mode:                  "enforce" | "warn-only" | "shadow" | "audit-only" | "log-only" | "canary",',
  '  audit:                 b.audit,',
  '  observability:         b.observability,',
  '  forensicEvidenceStore: { put }, forensicSnippetBytes: number,',
  '  cache:                 b.cache, cacheTtlMs: number,',
  '  maxRuntimeMs:          number,',
  '}</code></pre>',
  '<p>Returns a <code>b.gateContract</code>-shaped gate. Reads <code>ctx.identifier</code> (or <code>ctx.arg</code>); empty / missing argument resolves to <code>{ ok: true, action: "serve" }</code>.</p>',
  '<pre><code class="language-javascript">var shellGate = b.guardShell.gate({ profile: "strict", audit: b.audit });',
  '',
  'app.post("/admin/run-job", async function (req, res) {',
  '  var rv = await shellGate.check({ identifier: req.body.target });',
  '  if (!rv.ok) return res.status(400).json({ refused: rv.issues });',
  '  // ... pass req.body.target to spawn (still use array args + shell:false) ...',
  '});</code></pre>',

  '<h2 id="threat-catalog">Threat catalog <a class="anchor" href="#threat-catalog">#</a></h2>',
  '<ul>',
  '<li><strong>POSIX shell metacharacters</strong> — <code>;</code>, <code>&amp;</code>, <code>|</code>, <code>&lt;</code>, <code>&gt;</code>, <code>(</code>, <code>)</code>, <code>{</code>, <code>}</code>, <code>[</code>, <code>]</code>, <code>*</code>, <code>?</code>, <code>~</code>, <code>!</code>, <code>#</code>, <code>\\</code>, single + double quotes.</li>',
  '<li><strong>Backtick command substitution</strong> — <code>`whoami`</code> universally refused.</li>',
  '<li><strong>$(...) and ${VAR}</strong> — POSIX shell command + parameter substitution universally refused.</li>',
  '<li><strong>Process substitution</strong> — <code>&lt;(...)</code> / <code>&gt;(...)</code> Bash process substitution universally refused.</li>',
  '<li><strong>cmd.exe metacharacters</strong> — <code>&amp;</code>, <code>|</code>, <code>&lt;</code>, <code>&gt;</code>, <code>^</code>, <code>%</code>, <code>"</code>, <code>\'</code>, <code>(</code>, <code>)</code>, <code>,</code>, <code>;</code>, <code>=</code>.</li>',
  '<li><strong>Newline / NUL injection</strong> — line-splitting in scripts; universally refused.</li>',
  '<li><strong>$VAR parameter expansion</strong> — bare-form refused at strict; audited at balanced.</li>',
  '<li><strong>Leading-hyphen option-flag injection</strong> — operator may opt-in to refuse args starting with <code>-</code> to defend against <code>-rf</code> / <code>--exec</code> shapes that are interpreted as option flags by the target binary.</li>',
  '<li><strong>BIDI / null / control / zero-width</strong> — universal-refuse same codepoint catalog as the rest of the family.</li>',
  '</ul>',

  '<h2 id="profiles">Profiles <a class="anchor" href="#profiles">#</a></h2>',
  '<p><strong>strict</strong> — refuse everything in the catalog.</p>',
  '<p><strong>balanced</strong> — universal-refuse class still refused (substitutions / newline / BIDI / null); audit POSIX + cmd metacharacters + leading hyphen.</p>',
  '<p><strong>permissive</strong> — universal-refuse class still refused; metacharacters audited; allow leading hyphen.</p>',

  '<h2 id="compliance">Compliance postures <a class="anchor" href="#compliance">#</a></h2>',
  '<p><strong>hipaa</strong> / <strong>pci-dss</strong> / <strong>soc2</strong> — strict overlay + forensic snapshots (256 / 256 / 512 bytes).</p>',
  '<p><strong>gdpr</strong> — balanced overlay + 128-byte forensic snippets.</p>',

  ].join("\n"),
};
