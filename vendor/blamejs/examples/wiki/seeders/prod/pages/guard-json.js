"use strict";

module.exports = {
  slug:  "guard-json",
  title: "JSON content safety",
  body: [
  '<h1>JSON content safety</h1>',
  '<p><code>b.guardJson</code> is the JSON content-safety primitive. Threat catalog grounded in current research (CVE-2025-55182 React/Next.js Server Functions deserialization → RCE; CVE-2025-57820 + CVE-2026-30226 Svelte devalue prototype pollution; CVE-2026-35209 defu prototype pollution; CVE-2026-28794 @orpc/client deserialization; CVE-2025-13465 Lodash prototype-chain path traversal; CVE-2025-25014 Kibana prototype pollution → RCE; CVE-2024-38984 json-override; CVE-2022-42743 deep-parse-json; GHSA-9c47-m6qq-7p4h JSON5). Auto-registers into <code>b.guardAll</code>.</p>',

  '<h2 id="validate">b.guardJson.validate(input, opts) <a class="anchor" href="#validate">#</a></h2>',
  '<pre><code class="language-javascript">{',
  '  profile:                "strict" | "balanced" | "permissive",',
  '  compliancePosture:      "hipaa" | "pci-dss" | "gdpr" | "soc2",',
  '  pollutionPolicy:        "reject" | "strip" | "audit" | "allow",',
  '  duplicateKeyPolicy:     "reject" | "audit" | "allow",',
  '  nanInfinityPolicy:      "reject" | "audit" | "allow",',
  '  commentPolicy:          "reject" | "audit" | "allow",',
  '  trailingCommaPolicy:    "reject" | "audit" | "allow",',
  '  json5SyntaxPolicy:      "reject" | "audit" | "allow",',
  '  bomPolicy:              "reject" | "strip" | "allow",',
  '  bidiPolicy:             "reject" | "strip" | "audit" | "allow",',
  '  controlPolicy:          "reject" | "strip" | "audit" | "allow",',
  '  nullBytePolicy:         "reject" | "strip" | "audit" | "allow",',
  '  zeroWidthPolicy:        "reject" | "strip" | "audit" | "allow",',
  '  numericPrecisionPolicy: "reject" | "audit" | "allow",',
  '  requireTopLevelKeyAllowlist: boolean,',
  '  topLevelKeyAllowlist:   string[],          // optional',
  '  maxBytes:               number,',
  '  maxDepth:               number,',
  '  maxKeysPerObject:       number,',
  '  maxArrayLength:         number,',
  '  maxStringLength:        number,',
  '  maxTotalNodes:          number,',
  '}</code></pre>',
  '<p>Returns <code>{ ok, issues }</code>. Issue kinds: <code>prototype-pollution-key</code>, <code>duplicate-key</code>, <code>nan-infinity</code>, <code>comment-line</code>, <code>comment-block</code>, <code>trailing-comma</code>, <code>single-quoted-key</code>, <code>hex-literal</code>, <code>bom-leading</code>, <code>bom-mid-stream</code>, <code>bidi-override</code>, <code>null-byte</code>, <code>control-char</code>, <code>zero-width</code>, <code>numeric-precision-loss</code>, <code>depth-cap</code>, <code>key-count-cap</code>, <code>array-length-cap</code>, <code>string-too-long</code>, <code>node-count-cap</code>, <code>top-level-key-not-allowlisted</code>, <code>parse-failed</code>, <code>too-large</code>.</p>',
  '<pre><code class="language-javascript">b.guardJson.validate(\'{"name":"alice","age":30}\', { profile: "strict" });',
  '// → { ok: true, issues: [] }',
  '',
  '// Hostile: prototype pollution. After JSON.parse, __proto__ disappears',
  '// from Object.keys() (it routes through the prototype setter), so the',
  '// guard catches it at the SOURCE level — before any downstream parser',
  '// sees the input.',
  'b.guardJson.validate(\'{"__proto__":{"polluted":true}}\', { profile: "strict" });',
  '// → ok=false; issues include prototype-pollution-key',
  '</code></pre>',

  '<h2 id="parse">b.guardJson.parse(input, opts) <a class="anchor" href="#parse">#</a></h2>',
  '<pre><code class="language-javascript">{',
  '  profile:                "strict" | "balanced" | "permissive",',
  '  pollutionPolicy:        "reject" | "strip" | "audit" | "allow",',
  '  duplicateKeyPolicy:     "reject" | "audit" | "allow",',
  '  nanInfinityPolicy:      "reject" | "audit" | "allow",',
  '  commentPolicy:          "reject" | "audit" | "allow",',
  '  trailingCommaPolicy:    "reject" | "audit" | "allow",',
  '  json5SyntaxPolicy:      "reject" | "audit" | "allow",',
  '  bomPolicy:              "reject" | "strip" | "allow",',
  '  bidiPolicy:             "reject" | "strip" | "audit" | "allow",',
  '  controlPolicy:          "reject" | "strip" | "audit" | "allow",',
  '  nullBytePolicy:         "reject" | "strip" | "audit" | "allow",',
  '  zeroWidthPolicy:        "reject" | "strip" | "audit" | "allow",',
  '  maxBytes:               number,',
  '  maxDepth:               number,',
  '}</code></pre>',
  '<p>Parses input through <code>b.safeJson.parse</code> (which strips poisoned keys via reviver) AND adds the source-level pre-parse checks. Throws <code>GuardJsonError</code> on critical issues per the policy vocabulary (reject = throw / strip = silently remove / audit = parse but emit).</p>',
  '<pre><code class="language-javascript">b.guardJson.parse(\'{"name":"alice"}\', { profile: "strict" });',
  '// → { name: "alice" }',
  '',
  '// strict throws on prototype-pollution source.',
  'try { b.guardJson.parse(\'{"__proto__":{"x":1}}\', { profile: "strict" }); }',
  'catch (e) { e.code; }    // "json.prototype-pollution"',
  '',
  '// balanced strips silently.',
  'b.guardJson.parse(\'{"a":1,"__proto__":{"x":1}}\', { profile: "balanced" });',
  '// → { a: 1 }   — __proto__ removed, prototype not polluted',
  '</code></pre>',

  '<h2 id="gate">b.guardJson.gate(opts) <a class="anchor" href="#gate">#</a></h2>',
  '<pre><code class="language-javascript">{',
  '  profile:               "strict" | "balanced" | "permissive",',
  '  compliancePosture:     "hipaa" | "pci-dss" | "gdpr" | "soc2",',
  '  mode:                  "enforce" | "warn-only" | "shadow" | "audit-only" | "log-only" | "canary",',
  '  audit:                 b.audit,',
  '  observability:         b.observability,',
  '  forensicEvidenceStore: { put }, forensicSnippetBytes: number,',
  '  cache:                 b.cache, cacheTtlMs: number,',
  '  maxRuntimeMs:          number,',
  '  beforeCheck/afterCheck/onIssue/onSanitize/onRefuse/onAudit: hooks,',
  '}</code></pre>',
  '<p>Returns a <code>b.gateContract</code>-shaped gate. Auto-routes via <code>b.guardAll</code>\'s contentTypeMux when the request Content-Type is <code>application/json</code> / <code>application/ld+json</code> / <code>application/vnd.api+json</code>. As of v0.7.12, every <code>b.fileUpload</code> + <code>b.staticServe</code> deploy gets this gate at strict profile by default — no explicit wiring required.</p>',
  '<pre><code class="language-javascript">var jsonGate = b.guardJson.gate({ profile: "strict", audit: b.audit });',
  '</code></pre>',

  '<h2 id="threat-catalog">Threat catalog <a class="anchor" href="#threat-catalog">#</a></h2>',
  '<ul>',
  '<li><strong>Prototype pollution (source-level)</strong> — <code>__proto__</code> / <code>constructor</code> / <code>prototype</code> keys at any depth. Detected at the source-text level because after <code>JSON.parse</code>, <code>__proto__</code> routes through the prototype setter and disappears from <code>Object.keys()</code> — a post-parse tree walk misses the most dangerous key in the catalog. CVE-2025-55182 React Server Functions class.</li>',
  '<li><strong>Array-prototype pollution</strong> — non-numeric keys on array values (Svelte devalue CVE-2025-57820 class).</li>',
  '<li><strong>Depth bombs</strong> — nested arrays/objects past the configured cap.</li>',
  '<li><strong>Breadth / key-count bombs</strong> — single object with massive key count.</li>',
  '<li><strong>Duplicate keys</strong> — RFC 8259 says SHOULD-unique; <code>JSON.parse</code> silently last-wins, letting attackers smuggle duplicate-key payloads past validation that ran on the first occurrence (Bishop Fox JSON-interoperability research).</li>',
  '<li><strong>NaN / Infinity / -Infinity / undefined</strong> — RFC 8259 forbids; lenient parsers (JSON5 / lenient libraries) accept.</li>',
  '<li><strong>Comments</strong> — single-line and block forms; RFC 8259 forbids.</li>',
  '<li><strong>Trailing commas</strong> — RFC 8259 forbids.</li>',
  '<li><strong>JSON5 syntax</strong> — single-quoted keys, hex literals, unquoted keys.</li>',
  '<li><strong>BOM injection</strong> — leading or mid-stream U+FEFF.</li>',
  '<li><strong>Bidi / null / control chars / zero-width</strong> — same codepoint catalog as guard-csv/html/svg.</li>',
  '<li><strong>Numeric precision loss</strong> — integer values above <code>Number.MAX_SAFE_INTEGER</code> silently lose precision when round-tripped.</li>',
  '<li><strong>Top-level-key allowlist</strong> — operator declares allowed top-level keys; unauthorized keys refused.</li>',
  '<li><strong>Anti-DoS caps</strong> — total document size, depth, keys-per-object, array length, string length, total node count.</li>',
  '</ul>',

  '<h2 id="profiles">Profiles <a class="anchor" href="#profiles">#</a></h2>',
  '<p><strong>strict</strong> — refuse every threat (pollution / duplicates / NaN / comments / trailing commas / JSON5 syntax / BOM / bidi / null / control / zero-width / numeric precision); 2 MiB document cap; depth 8; 256 keys/object; 1024 array items; 8 KiB strings; 8192 total nodes. Suitable for ANY operator-supplied JSON where the operator wants strong defense.</p>',
  '<p><strong>balanced</strong> — strip pollution / BOM / bidi / control / null / zero-width; audit duplicates / comments / trailing commas / JSON5 / numeric precision; refuse NaN unconditionally; 8 MiB cap; depth 32; 4096 keys/object; 65536 array items.</p>',
  '<p><strong>permissive</strong> — audit most threats; refuse only null bytes outright; 64 MiB cap; depth 64; 65536 keys/object; 1048576 array items.</p>',

  '<h2 id="compliance">Compliance postures <a class="anchor" href="#compliance">#</a></h2>',
  '<p><strong>hipaa</strong> / <strong>pci-dss</strong> / <strong>soc2</strong> — strict overlay + forensic snapshots (256 / 256 / 512 bytes).</p>',
  '<p><strong>gdpr</strong> — balanced overlay + 128-byte forensic snippets.</p>',

  ].join("\n"),
};
